# BIT-RF-2026-001 — Requerimientos Funcionales

**GECELCA S.A. E.S.P. — Gerencia de Generación**
**Módulo de Bitácoras de Planta — Sistema de registro operativo y bitácoras dinámicas**

| Campo | Valor |
|---|---|
| Código | BIT-RF-2026-001 |
| Versión | 2.0 |
| Fecha | 2026-07-27 |
| Estado | Borrador para revisión |
| Clasificación | Uso interno — Confidencial |
| Documento base | BIT-MODBD-2026-001 (Modelo de Base de Datos) |
| Autor | Equipo de Ingeniería de Software |

**Alcance:** Este documento especifica los requerimientos funcionales del Módulo de Bitácoras de Planta, un sistema web de registro operativo para el personal de las plantas térmicas de Gecelca. El módulo expone adicionalmente un contrato de datos hacia el Dashboard de Generación en Tiempo Real mediante la tabla puente de autorizaciones. Toda referencia a estructuras de datos remite al documento BIT-MODBD-2026-001.

---

## Tabla de Contenido

1. [Introducción](#1-introducción)
2. [Contexto del sistema](#2-contexto-del-sistema)
3. [Arquitectura funcional](#3-arquitectura-funcional)
4. [Requerimientos funcionales](#4-requerimientos-funcionales)
5. [Requerimientos no funcionales](#5-requerimientos-no-funcionales)
6. [Matriz de trazabilidad RF ↔ Modelo de datos](#6-matriz-de-trazabilidad-rf--modelo-de-datos)
7. [Reglas de negocio críticas](#7-reglas-de-negocio-críticas)
8. [Supuestos, restricciones y exclusiones](#8-supuestos-restricciones-y-exclusiones)
9. [Criterios de aceptación global](#9-criterios-de-aceptación-global)
10. [Historial de versiones](#10-historial-de-versiones)

---

## 1. Introducción

### 1.1 Propósito del documento

Este documento especifica los requerimientos funcionales del Módulo de Bitácoras de Planta de Gecelca. Está dirigido a ingenieros de desarrollo, analistas de pruebas, arquitectos de software y responsables operativos. Su objetivo es establecer un entendimiento unívoco de las capacidades funcionales, reglas de negocio y restricciones del sistema, sirviendo como contrato entre el área usuaria (Gerencia de Generación) y el equipo de construcción.

Este documento se complementa estrictamente con **BIT-MODBD-2026-001** (Modelo de Base de Datos). Cualquier estructura de datos mencionada aquí remite a su definición formal en dicho documento, y no debe interpretarse como fuente autoritativa por sí sola.

### 1.2 Alcance funcional

El Módulo cubre los siguientes dominios:

- Autenticación y selección de contexto operativo (planta y cargo).
- **Gestión de sesiones activas con TTL de 5 minutos**, heartbeat periódico, reanudación al recargar la página y cierre explícito vía `sendBeacon` al cerrar pestaña. Permite determinar en cualquier momento quién es el Jefe de Turno vigente y qué ingenieros están en turno.
- Creación, edición y eliminación de registros operativos en bitácoras temáticas.
- **Captura de snapshots de los roles vigentes** (ingenieros en turno, JdTs activos, jefe de planta) al momento del registro, como JSON embebido, para preservar trazabilidad histórica sin FK frágiles.
- Renderizado dinámico de formularios para bitácoras "especiales" cuyos campos no se conocen en tiempo de compilación.
- Cierre de bitácoras por parte del Jefe de Turno, con archivado atómico al histórico.
- Consulta y búsqueda de registros históricos con filtros múltiples.
- Exposición de autorizaciones de despacho al Dashboard de Generación para supresión controlada del cálculo de desviación.

Quedan explícitamente fuera del alcance de esta versión:

- Integración con Active Directory o Keycloak (se usará autenticación simple por email+contraseña hasta nuevo aviso).
- Notificaciones automáticas a ingenieros y Jefe de Turno sobre bitácoras sin cerrar.
- Exportación a PDF o Excel de bitácoras cerradas.
- Gestión de adjuntos (imágenes, documentos) en los registros.
- Reglas de aprobación multinivel; el cierre es una única acción ejecutada por el Jefe de Turno.

### 1.3 Definiciones y acrónimos

| Término | Definición |
|---|---|
| Bitácora | Registro cronológico de eventos operativos agrupados por tema (Caldera, Turbina, etc.). Cada bitácora es una entidad lógica con su propio catálogo en `lov_bit.bitacora`. |
| JdT | Jefe de Turno. Cargo con privilegios de cierre y de creación en bitácoras especiales (Disponibilidad y Autorizaciones). |
| Turno | Periodo laboral de 12 horas. **Turno 1 (T1): [06:00, 18:00)**; **Turno 2 (T2): [18:00, 06:00)** — T2 cruza medianoche (hora Colombia, UTC−5, sin horario de verano). El turno se fija al login (D-003), no se recalcula por hora del registro. Desde D-045 es una entidad explícita (`bitacora.turno_unidad`) con ciclo de vida PROGRAMADO→ABIERTO→CERRADO. *(Corrige la definición invertida previa 00:00–11:59 / 12:00–23:59 — hallazgo D1 de la auditoría 2026-07-04.)* |
| Registro activo | Registro operativo editable, almacenado en `bitacora.registro_activo`, cuyo estado es `'borrador'` o `'cerrado'`. |
| Registro histórico | Registro inmutable archivado en `bitacora.registro_historico` tras el cierre de su bitácora por el JdT. |
| Cierre | Acción exclusiva del JdT que archiva los registros activos de una bitácora al histórico en una transacción atómica. |
| Autorización | Registro creado en la bitácora Autorizaciones (código AUTH) por el JdT, que habilita un valor de despacho para un periodo/planta específicos y suprime el cálculo de desviación en el Dashboard. |
| Dashboard | Sistema externo (Dashboard de Generación en Tiempo Real) que consume vía REST la tabla `bitacora.autorizacion_dashboard`. |
| `campos_extra` | Columna `NVARCHAR(MAX)` que almacena un documento JSON con campos dinámicos específicos de cada bitácora. Su estructura se valida en la capa de aplicación contra el esquema definido en `lov_bit.bitacora.definicion_campos`. |
| `definicion_campos` | Documento JSON almacenado en `lov_bit.bitacora` que describe los campos dinámicos de una bitácora especial. El frontend lo interpreta para renderizar formularios sin cambios de código. |
| **Snapshot de rol** | **Documento JSON `[{usuario_id, nombre_completo}, ...]` almacenado en columnas `ingenieros_snapshot`, `jdts_snapshot`, `jefes_snapshot`. Congela la lista de personas que ocupaban un rol al momento del registro. No es FK — sobrevive a bajas, cambios de rol o re-asignaciones.** |
| **TTL de sesión** | **Ventana temporal (5 minutos) durante la cual una sesión se considera viva basándose en `ultima_actividad`. Permite reanudar sesiones tras F5 sin re-login. Implementado vía `DATEADD(MINUTE, -5, GETDATE())` en backend.** |
| **`authReady`** | **Flag booleano del cliente que queda `true` tras resolverse el intento de reanudación de sesión al montar la aplicación. Gatea la ejecución de cualquier request autenticado para evitar carreras con `/api/auth/resume`.** |

### 1.4 Referencias normativas

- BIT-MODBD-2026-001 — Modelo de Base de Datos del módulo de Bitácoras.
- Manual operativo de la Gerencia de Generación de Gecelca (vigente).
- Estándares de desarrollo interno de Gecelca: Node.js ESM, SQL Server 2019+, React 19.

---

## 2. Contexto del sistema

### 2.1 Problema que resuelve

Las plantas térmicas Gecelca 3 y Gecelca 3.2 operan con turnos 24/7 en los que distintos ingenieros (operación, planta de agua) y un Jefe de Turno deben documentar eventos operativos de forma sistemática. Históricamente, este registro se ha realizado en hojas de cálculo, documentos de Word o bitácoras físicas, lo que implica: trazabilidad pobre entre turnos, imposibilidad de búsqueda histórica eficiente, riesgo de pérdida o alteración de registros, y ausencia de un hilo común entre los registros operativos y los sistemas de monitoreo en línea.

El Módulo de Bitácoras resuelve estos problemas centralizando la captura de eventos operativos en una base de datos relacional, con control de estados, auditoría completa y una interfaz web accesible desde cualquier puesto de la sala de control. Además, establece un canal formal mediante el cual el Jefe de Turno puede autorizar desviaciones de despacho que deben reflejarse automáticamente en el Dashboard de Generación.

### 2.2 Actores del sistema

El sistema reconoce cuatro cargos operativos, definidos en `lov_bit.cargo`. La tabla `lov_bit.cargo_bitacora_permiso` materializa la matriz de acceso.

| Cargo | Permisos globales | Notas |
|---|---|---|
| Jefe de Turno | Lectura en todas las bitácoras; creación en Disponibilidad y Autorizaciones; cierre de bitácoras. | Único actor con privilegio de cierre. Su sesión activa alimenta el snapshot `jdts_snapshot` que se autocompleta en cada registro creado. |
| Ingeniero de Operación | Lectura en todas; creación en todas las bitácoras generales (excluye Disponibilidad, Autorizaciones y Planta de Agua). | Es el cargo de mayor uso esperado; produce el grueso de los registros diarios. |
| Ingeniero de Planta de Agua | Creación exclusiva en la bitácora de Planta de Agua. | Cargo especializado. No tiene visibilidad sobre otras bitácoras. |
| Gerente de Producción | Lectura en todas las bitácoras; sin privilegio de creación ni cierre. | Marcado con `solo_lectura=1` en `lov_bit.cargo`. El frontend deshabilita todos los controles de escritura. |

### 2.3 Usuarios predefinidos

Dos usuarios cumplen roles fijos independientes del esquema de cargos:

- **Ernesto Muñoz** — marcado con `es_jefe_planta=1` en `lov_bit.usuario`. Se captura en el snapshot `jefes_snapshot` de todos los registros, independientemente de quién los cree. Es el único usuario con esta marca.
- **Omar Fedullo** — marcado con `es_jdt_default=1`. Actúa como JdT de fallback cuando ningún usuario con cargo "Jefe de Turno" tiene sesión activa dentro del TTL en la planta correspondiente.

### 2.4 Plantas soportadas

Dos plantas están registradas en `lov_bit.planta` con los identificadores que comparten con el Dashboard de Generación para garantizar consistencia entre sistemas:

- **GEC3** — Gecelca 3.
- **GEC32** — Gecelca 3.2.

Todas las bitácoras aplican a ambas plantas. La separación por planta se realiza mediante la columna `planta_id` en cada registro, seleccionada durante el flujo de inicio de sesión.

---

## 3. Arquitectura funcional

### 3.1 Componentes de alto nivel

El módulo se estructura en tres componentes lógicos que interactúan vía HTTP y SQL:

| Componente | Tecnología | Responsabilidad |
|---|---|---|
| Frontend web | React 19 + Vite + Tailwind | Interfaz de usuario. Login, selección de contexto, grilla de registros, cierre, búsqueda histórica. Renderiza formularios dinámicos desde `definicion_campos`. Gate `authReady` para evitar carreras post-reload. |
| Backend API | Node.js ESM + `http` nativo + `mssql` | Exposición de endpoints REST. Validación, control de permisos, resolución de snapshots desde sesiones activas con TTL, ejecución de transacciones de cierre. |
| Base de datos | SQL Server 2019+ | Esquemas `lov_bit` (catálogos) y `bitacora` (transaccional). Ver documento BIT-MODBD-2026-001 para DDL completo. |

### 3.2 Flujo de datos principal

1. El usuario inicia sesión con email y contraseña contra `lov_bit.usuario`.
2. Selecciona planta y cargo. El backend inserta una fila en `bitacora.sesion_activa` con el turno calculado (1 o 2 según hora Colombia) y `ultima_actividad=GETDATE()`.
3. El frontend guarda `{user, sesion}` en `sessionStorage` (aislado por pestaña) y arranca un heartbeat cada 60 s que bumpea `ultima_actividad` en el servidor.
4. El frontend carga el catálogo de bitácoras filtrado por `lov_bit.cargo_bitacora_permiso` según el cargo seleccionado.
5. Al crear un registro, el frontend envía los tres campos manuales (`fecha_evento`, `turno`, `detalle`), el tipo de evento seleccionado, y el JSON de `campos_extra` si la bitácora es especial. El backend:
   - Fija `creado_por` con el `usuario_id` derivado de la sesión (único FK vivo a `lov_bit.usuario`).
   - Resuelve y guarda los snapshots JSON de roles (`ingenieros_snapshot`, `jdts_snapshot`, `jefes_snapshot`) leyendo `bitacora.sesion_activa` con filtro TTL (ver 3.3).
   - Completa los campos de auditoría.
6. El registro se inserta en `bitacora.registro_activo` con `estado='borrador'`.
7. Si la bitácora es **Autorizaciones** (código AUTH), el backend también inserta una fila en `bitacora.autorizacion_dashboard` extrayendo `periodo` y `valor_autorizado_mw` del JSON `campos_extra`, guardando también los snapshots de JdT y jefe vigentes.
8. Cuando el JdT cierra la bitácora, el backend ejecuta una transacción: `INSERT` en `bitacora.registro_historico` copiando los registros activos junto con los snapshots ya congelados y los campos de cierre (`cerrado_por`, `cerrado_en`, `fecha_cierre_operativo`), seguido de `DELETE` en `bitacora.registro_activo`.
9. El Dashboard de Generación consulta periódicamente `GET /api/autorizaciones` para suprimir desviaciones en los periodos autorizados.

### 3.3 Resolución de snapshots de roles

La determinación de los roles vigentes al momento de cada registro sigue reglas deterministas que el backend debe ejecutar en el mismo punto de inserción:

- **Ingenieros en turno (`ingenieros_snapshot`):** Lista JSON de todos los usuarios con cargo en `('Ingeniero de Operación', 'Ingeniero de Planta de Agua')` y sesión en `bitacora.sesion_activa` con `activa=1`, `planta_id=@planta` y `ultima_actividad > DATEADD(MINUTE, -5, GETDATE())`. Forma: `[{usuario_id, nombre_completo}, ...]`.
- **Jefes de Turno vigentes (`jdts_snapshot`):** Misma consulta filtrando cargo `'Jefe de Turno'`. Si resulta vacía, se emite un snapshot con el usuario marcado `es_jdt_default=1` (fallback Omar Fedullo).
- **Jefe de planta (`jefes_snapshot`):** Lista JSON con el/los usuarios marcados `es_jefe_planta=1` en `lov_bit.usuario`. En la semilla actual es un singleton (Ernesto Muñoz).
- **Turno:** calculado a partir de la hora Colombia (UTC−5). Valores 0–11 → turno 1; valores 12–23 → turno 2.

Los snapshots son inmutables una vez persistidos. Las bajas, reasignaciones o cambios de cargo posteriores NO alteran registros históricos ni activos ya creados.

### 3.4 Ciclo de vida de la sesión

- **Login → Selección de contexto:** crea la fila en `bitacora.sesion_activa` con `activa=1`, `inicio_sesion=GETDATE()`, `ultima_actividad=GETDATE()`.
- **Heartbeat:** cada 60 s el cliente envía `POST /api/auth/heartbeat`; el backend actualiza `ultima_actividad=GETDATE()`.
- **Cada request autenticado:** el middleware `loadSession` valida el header `X-Sesion-Id` exigiendo `activa=1` y `ultima_actividad > DATEADD(MINUTE, -5, GETDATE())`; luego bumpea `ultima_actividad` en fire-and-forget.
- **Cerrar pestaña (`pagehide`):** el cliente emite `navigator.sendBeacon('/api/auth/logout')`; el servidor marca `activa=0`. Garantiza que al abrir una pestaña nueva se exija re-login.
- **Recargar la página (F5):** el `pagehide` dispara el beacon → `activa=0`. Al montar la app nueva, `useAuth` lee `sessionStorage`; si encuentra `sesion_id`, llama `POST /api/auth/resume` con `{ sesion_id }`. El backend reactiva (`activa=1`, `ultima_actividad=GETDATE()`) solo si la sesión está dentro del TTL. Durante esta ventana, el flag `authReady=false` gatea a `useCatalogos` y al resto del árbol para que ningún request salga antes del `resume`.
- **Arranque del backend:** `initDB()` ejecuta un barrido inicial que pone `activa=0` en todas las sesiones huérfanas cuya `ultima_actividad` esté fuera del TTL.

---

## 4. Requerimientos funcionales

Los requerimientos se codifican como **RF-XXX**. Cada uno incluye: descripción, actores involucrados, precondiciones, postcondiciones y referencia a la estructura de datos en BIT-MODBD-2026-001 cuando aplique.

### 4.1 Autenticación y gestión de sesión

#### RF-001 — Inicio de sesión por credenciales

| | |
|---|---|
| **Descripción** | El sistema debe permitir que un usuario inicie sesión proporcionando email y contraseña. La validación se realiza contra `lov_bit.usuario` verificando además que `activo=1`. |
| **Actores** | Todos los cargos. |
| **Precondiciones** | El usuario existe en `lov_bit.usuario` y su campo `activo` es 1. |
| **Postcondiciones** | El frontend recibe los datos del usuario (sin `password_hash`) y los mantiene en memoria para el siguiente paso. |
| **Reglas** | Credenciales inválidas retornan HTTP 401 con mensaje genérico (no revelar si el email existe). |

#### RF-002 — Selección de contexto operativo

| | |
|---|---|
| **Descripción** | Tras autenticarse, el usuario debe seleccionar la planta (GEC3 o GEC32) y su cargo operativo entre los disponibles en `lov_bit.cargo`. |
| **Actores** | Todos los cargos. |
| **Precondiciones** | RF-001 completado. |
| **Postcondiciones** | Se inserta un registro en `bitacora.sesion_activa` con `usuario_id`, `planta_id`, `cargo_id`, turno calculado automáticamente, `inicio_sesion=GETDATE()`, `ultima_actividad=GETDATE()` y `activa=1`. |
| **Reglas** | El turno se calcula como 1 si la hora Colombia < 12, 2 en caso contrario. El usuario puede re-seleccionar cargo durante la misma sesión (se crea una nueva fila; la anterior queda con `activa=1` hasta logout explícito o expiración del TTL). |

#### RF-003 — Heartbeat de sesión

| | |
|---|---|
| **Descripción** | El frontend debe enviar un heartbeat cada 60 segundos para actualizar `sesion_activa.ultima_actividad`. Esto mantiene la sesión dentro del TTL mientras la aplicación esté abierta y permite detectar sesiones abandonadas. |
| **Actores** | Sistema (automático mientras la aplicación esté abierta). |
| **Endpoint** | `POST /api/auth/heartbeat`. |
| **Postcondiciones** | Campo `ultima_actividad` actualizado a `GETDATE()` en `bitacora.sesion_activa`. |

#### RF-004 — Cierre explícito de sesión

| | |
|---|---|
| **Descripción** | El usuario puede cerrar sesión desde el header. Al hacerlo, se actualiza `bitacora.sesion_activa.activa=0`. |
| **Actores** | Todos los cargos. |
| **Endpoint** | `POST /api/auth/logout`. |
| **Postcondiciones** | La sesión ya no aparece en consultas de sesiones activas ni influye en la resolución del JdT en turno ni en los snapshots. |

#### RF-005 — Cierre de sesión al abandonar la pestaña

| | |
|---|---|
| **Descripción** | Al disparar el evento `pagehide` (cierre de pestaña, navegación fuera, crash, reload), el frontend debe emitir un `navigator.sendBeacon('/api/auth/logout', { sesion_id })` para garantizar que la sesión quede `activa=0` incluso si el proceso del navegador termina. |
| **Actores** | Sistema (frontend automático). |
| **Reglas** | Solo se usa `sendBeacon` (no `fetch`) porque el navegador no garantiza la entrega de otras solicitudes durante `pagehide`. |
| **Postcondiciones** | `bitacora.sesion_activa.activa = 0`. |

#### RF-006 — Reanudación de sesión al recargar la página

| | |
|---|---|
| **Descripción** | Cuando la aplicación monta y encuentra un `sesion_id` en `sessionStorage`, debe intentar reanudar la sesión invocando `POST /api/auth/resume` con `{ sesion_id }` antes de permitir cualquier otro request. |
| **Actores** | Sistema (frontend automático al montar). |
| **Precondiciones** | Hay un `sesion_id` persistido en `sessionStorage` (misma pestaña, reload o navegación interna). |
| **Postcondiciones** | Si la sesión existe y está dentro del TTL, se reactiva (`activa=1`, `ultima_actividad=GETDATE()`) y la UI se restaura con el contexto previo. Si no, se limpia `sessionStorage` y se fuerza re-login. |
| **Reglas** | El frontend expone el flag `authReady` que permanece `false` mientras la operación no concluya. Ningún hook o efecto que haga requests autenticados debe ejecutarse antes de `authReady===true`. Durante la ventana, la UI muestra un loader minimalista ("Cargando…"). |

#### RF-007 — Enforcement de TTL en el backend

| | |
|---|---|
| **Descripción** | El middleware `loadSession` debe rechazar (HTTP 401) cualquier request autenticado cuyo `sesion_id` apunte a una fila con `activa=0` o con `ultima_actividad` fuera del TTL de 5 minutos. |
| **Actores** | Sistema (backend). |
| **Implementación** | Predicado SQL: `activa=1 AND ultima_actividad > DATEADD(MINUTE, -5, GETDATE())`. |
| **Postcondiciones** | Requests válidos: bump de `ultima_actividad` en fire-and-forget. Requests rechazados: 401 que el cliente traduce en logout. |
| **Notas** | Al arrancar, `initDB()` barre sesiones huérfanas (marcando `activa=0` donde `ultima_actividad` esté fuera del TTL) para mantener la tabla consistente. |

---

### 4.2 Catálogos y permisos

#### RF-010 — Listado de bitácoras por cargo

| | |
|---|---|
| **Descripción** | El sistema debe exponer al frontend únicamente las bitácoras para las cuales el cargo seleccionado tenga `puede_ver=1` en `lov_bit.cargo_bitacora_permiso`. Las bitácoras con `puede_crear=0` se muestran en modo solo lectura. |
| **Actores** | Todos los cargos. |
| **Reglas** | El Jefe de Turno ve las 10 bitácoras. El Ingeniero de Operación ve todas excepto Disponibilidad y Autorizaciones en modo escritura. El Ingeniero de Planta de Agua solo ve la bitácora de Planta de Agua. El Gerente ve todas en modo solo lectura. |

#### RF-011 — Catálogo dinámico de bitácoras

| | |
|---|---|
| **Descripción** | Las bitácoras se definen en la tabla `lov_bit.bitacora`. Añadir una nueva bitácora al sistema NO debe requerir cambios de código: basta con insertar una fila con su nombre, código, icono, orden y, si aplica, un JSON `definicion_campos`. |
| **Actores** | Administrador técnico (por ahora, mediante SQL directo). |
| **Reglas** | El frontend debe consumir el catálogo vía API y renderizar las tabs de bitácoras ordenadas por el campo `orden`. Ninguna bitácora debe estar hardcodeada en el código. |

#### RF-012 — Tipos de evento por bitácora

| | |
|---|---|
| **Descripción** | Cada bitácora tiene su propio catálogo de tipos de evento en `lov_bit.tipo_evento`. El frontend presenta un selector cuyo valor por defecto es el tipo marcado con `es_default=1` (típicamente 'Evento General'). |
| **Actores** | Todos los cargos con permiso de creación. |
| **Reglas** | Todas las bitácoras tienen al menos el tipo 'Evento General'. Las bitácoras especiales pueden definir tipos adicionales (ej.: Disponibilidad tiene 'Cambio de Estado', 'Redespacho', 'Sincronización'). |

---

### 4.3 Gestión de registros operativos

#### RF-020 — Creación de registro general

| | |
|---|---|
| **Descripción** | El usuario puede crear un registro en una bitácora general (`formulario_especial=0`). El formulario presenta tres campos manuales: fecha/hora del evento, turno y detalle. Adicionalmente un selector de tipo de evento con default. |
| **Actores** | Jefe de Turno, Ingeniero de Operación, Ingeniero de Planta de Agua (en su bitácora). |
| **Precondiciones** | Sesión activa (dentro del TTL). El cargo tiene `puede_crear=1` para la bitácora objetivo. |
| **Postcondiciones** | Se inserta una fila en `bitacora.registro_activo` con `estado='borrador'`. El backend resuelve y persiste: (a) `creado_por` = `sesion.usuario_id` (único FK vivo a `lov_bit.usuario`); (b) los snapshots JSON `ingenieros_snapshot`, `jdts_snapshot`, `jefes_snapshot` según sección 3.3; (c) los campos de auditoría. |
| **Validaciones** | Detalle no vacío. Fecha de evento no futura más allá de 5 minutos desde `GETDATE()`. Turno en {1, 2}. `tipo_evento_id` pertenece a la bitácora. |

#### RF-021 — Creación de registro en bitácora especial

| | |
|---|---|
| **Descripción** | Para bitácoras con `formulario_especial=1`, el frontend renderiza dinámicamente el formulario adicional a partir del JSON `definicion_campos`. Los valores recolectados se serializan como JSON y se almacenan en la columna `campos_extra`. |
| **Actores** | Según permisos. Solo el JdT puede crear en Disponibilidad y Autorizaciones. |
| **Validaciones** | Todos los campos marcados como `requerido=true` en la definición deben estar presentes. Campos de tipo 'auto' se calculan en el backend según su regla (ejemplo: `codigo` en Disponibilidad se deriva del evento seleccionado: En Servicio=1, En Reserva=0, Indisponible=−1, Mantenimiento=−1 — ver D-024; los dos últimos comparten `codigo` y se distinguen por el string `evento`). |
| **Notas** | La validación del JSON ocurre en el backend; la base de datos solo garantiza que sea un `NVARCHAR(MAX)` válido. No hay `CHECK` constraints sobre el contenido del JSON por decisión de diseño documentada en BIT-MODBD-2026-001. Los snapshots de rol se persisten en paralelo, igual que RF-020. |

#### RF-022 — Edición de registro en borrador

| | |
|---|---|
| **Descripción** | **Únicamente el autor** de un registro (`creado_por`) puede editar los campos manuales y el JSON `campos_extra` mientras su estado sea `'borrador'` (D-049). |
| **Actores** | Solo el autor del registro, siempre que conserve `puede_crear=1` vigente en la bitácora y opere en su misma planta. Sin excepciones por cargo: el bypass histórico del JdT/Ing. de Operación (`puede_cerrar_turno`) se retiró — quien solo tiene `puede_ver` es estrictamente lectura. |
| **Reglas** | No se pueden editar los campos automáticos (`creado_por`, `creado_en`, `ingenieros_snapshot`, `jdts_snapshot`, `jefes_snapshot`). Los snapshots son **inmutables**: congelan el estado del momento de creación. Los campos `modificado_por` y `modificado_en` se actualizan con cada edición. |
| **Restricciones** | Un registro con `estado='cerrado'` no puede editarse por ninguna vía funcional. La API debe retornar HTTP 409 si se intenta, y HTTP 403 con `codigo='solo_autor'` si quien edita no es el autor. |

#### RF-023 — Eliminación de registro en borrador

| | |
|---|---|
| **Descripción** | El sistema permite eliminar un registro mientras esté en estado `'borrador'`. Si el registro originó una autorización (bitácora AUTH), la fila correspondiente en `bitacora.autorizacion_dashboard` se marca con `activa=0` antes del `DELETE`. |
| **Actores** | **Únicamente el autor** del registro, con `puede_crear=1` vigente en la bitácora (D-049). La excepción histórica del JdT se retiró: un no-autor recibe HTTP 403 con `codigo='solo_autor'`. |
| **Reglas** | La eliminación es física (`DELETE`) sobre `bitacora.registro_activo`. La autorización queda con `activa=0` (soft delete) para preservar la trazabilidad en el historial de cambios consultado por el Dashboard. |

#### RF-024 — Consulta de registros activos

| | |
|---|---|
| **Descripción** | El usuario puede listar los registros activos de una bitácora filtrada por planta. La respuesta incluye los datos del registro, los snapshots JSON de roles ya resueltos (`ingenieros_snapshot`, `jdts_snapshot`, `jefes_snapshot`), el nombre del autor `creado_por_nombre` (derivado de JOIN con `lov_bit.usuario`) y el nombre del tipo de evento. |
| **Actores** | Todos los cargos con `puede_ver=1` para la bitácora. |
| **Reglas** | Los resultados se ordenan por `fecha_evento` ascendente. El frontend muestra el estado (borrador/cerrado) con código de color y los botones de acción según permisos y estado. Los snapshots se muestran como listas legibles ("Juan Pérez, Ana López") sin necesidad de JOINs adicionales. |

---

### 4.4 Cierre de bitácoras

#### RF-030 — Cierre individual de bitácora — RETIRADO (D-042, 2026-07-03)

| | |
|---|---|
| **Estado** | **Eliminado.** El cierre individual por bitácora se retiró por completo (backend `POST /api/cierre/bitacora`, botón "Cerrar Turno" por pestaña, hook, tests). El único cierre operativo es el **cierre de turno** (RF-031). Se conserva el número de RF por trazabilidad de auditoría. Ver **D-042**. |

#### RF-031 — Cierre de turno

| | |
|---|---|
| **Descripción** | El Jefe de Turno o el Ingeniero de Operación (`puede_cerrar_turno=1`) cierra el turno de su planta en una sola acción, que **sella la entidad `bitacora.turno_unidad`** (ABIERTO→CERRADO) y, en la misma transacción, archiva al histórico los borradores de **todas** las bitácoras genéricas con registros activos atados a ese `turno_id`, congela la conformación desde `turno_participante` y activa el turno sucesor. Es el ÚNICO cierre del sistema. **Actualizado por D-045** (antes cierre masivo por planta de D-042; sigue siendo el único cierre). |
| **Actores** | Ingeniero Jefe de Turno o Ingeniero de Operación. También lo dispara el auto-cierre del sweeper al vencer la gracia sin personal/respuesta (`AUTO_SIN_PERSONAL`/`AUTO_SIN_RESPUESTA`). |
| **Precondiciones** | La sesión de app está activa y ligada a un `turno_unidad` ABIERTO. Endpoint `POST /api/turno/cerrar` (body `{turno_id, motivo?}`); estado en vivo vía `GET /api/turno/actual` + WS `/ws/turno-transicion`. *(Reemplaza `POST /api/cierre/masivo` + `GET /api/cierre/preview-masivo`, retirados en D-045.)* |
| **Postcondiciones** | Se sella la cabecera (`estado='CERRADO'`, `motivo_cierre`, `fin_real`, `cerrado_por`, `cerrado_en=SYSUTCDATETIME()`). Por cada bitácora atada al `turno_id`: `INSERT` en `bitacora.registro_historico` copiando los snapshots ya congelados + campos de cierre, luego `DELETE` de `bitacora.registro_activo`. Se congela `conformacion_turno` desde `turno_participante` y se emite un CIET `'Cierre de turno'`. |
| **Reglas** | Todo es **una** transacción atómica (`cerrarTurno` en `utils/turno-entidad.js`); dentro de una transacción `mssql` se prohíbe `Promise.all` — queries serializadas con `await`. DISP y MAND quedan excluidos (`codigo NOT IN ('DISP','MAND')`) por tener ciclo de vida propio; CIET también (oculta). Los registros se seleccionan por `turno_id` (no por ventana de tiempo inferida). El sweeper (`transicionarTurnosVencidos`) solo auto-cierra plantas reales `('GEC3','GEC32')`, nunca TST. |
| **Respuesta** | `{ turno_id, estado:'CERRADO', motivo_cierre, registros_archivados, conformacion_filas }`. |
| **Advertencias** | Al vencer el turno sin personal presente, el sistema muestra un modal bloqueante (`TurnoTransicionModal`) que ofrece **extender** o **cerrar**; si nadie decide dentro de la gracia, el sweeper auto-cierra con `AUTO_SIN_RESPUESTA`/`AUTO_SIN_PERSONAL`. |

#### RF-032 — Inmutabilidad del histórico

| | |
|---|---|
| **Descripción** | Los registros en `bitacora.registro_historico` son inmutables a nivel funcional. No existe endpoint para editar ni eliminar registros históricos. |
| **Reglas** | Cualquier corrección posterior se debe realizar mediante un nuevo registro complementario en la bitácora correspondiente del día actual, con referencia cruzada en el campo `detalle`. Esta regla es organizativa; el DDL no impone triggers de bloqueo de `UPDATE/DELETE` en esta versión. |
| **Excepciones** | Tres, todas **estructurales y jamás por acción de usuario** (no existe ni existirá endpoint que edite el histórico): **(1)** `reabrirTurno` (D-045) desarchiva por `turno_id` los registros de un turno reabierto, devolviéndolos a `registro_activo`. **(2)** La migración one-shot `F30.A1` (D-053) reasignó `bitacora_id`/`tipo_evento_id` al partir la bitácora de Sala de Mando por rol; corrió una única vez, en transacción, respaldando las filas afectadas en `bitacora.registro_historico_backup_D053` (residente, BIT-MODBD §4.11). **(3)** La migración one-shot `F32.A1` (D-056) agregó `campos_extra.lote_id` a los registros MAND previos —un GUID por fila, sin tocar valores, autores ni fechas— respaldando en `bitacora.registro_historico_backup_D056` / `registro_activo_backup_D056` (residentes). El backfill de `turno_id` de `F31.A1` (D-055) siguió el mismo criterio. Toda excepción futura exige el mismo trío: **ADR + one-shot transaccional + respaldo**. |

---

### 4.5 Consulta de históricos

#### RF-040 — Búsqueda en históricos con filtros múltiples

| | |
|---|---|
| **Descripción** | El usuario puede consultar registros históricos aplicando combinaciones de los siguientes filtros: planta, bitácora, ingeniero (búsqueda textual sobre `ingenieros_snapshot`), turno, rango de fechas (`fecha_desde`, `fecha_hasta`), tipo de evento, y búsqueda textual sobre el campo `detalle`. |
| **Actores** | Todos los cargos con `puede_ver=1` para la bitácora consultada. |
| **Reglas** | La consulta se apoya en la vista `bitacora.v_historico_busqueda`, que expone los snapshots como columnas ya formateadas (sin JOINs a `lov_bit.usuario` para roles que hayan cambiado). Los resultados se paginan (default: 50 filas por página) y se ordenan por `fecha_cierre_operativo DESC`, `fecha_evento DESC`. |
| **Respuesta** | Objeto JSON `{ data: [...], total, page, limit }`. |

#### RF-041 — Detalle de un registro histórico

| | |
|---|---|
| **Descripción** | Obtener un registro histórico específico por su identificador, incluyendo los snapshots JSON tal cual se persistieron al crear el registro. El frontend parsea los snapshots para mostrarlos como listas legibles. |
| **Reglas** | Retorna HTTP 404 si el registro no existe. Los nombres en los snapshots son los vigentes al momento de la creación del registro, no los actuales. |

#### RF-042 — Resumen diario de cierres

| | |
|---|---|
| **Descripción** | Dado `(planta, fecha)`, el sistema presenta el conteo de registros cerrados por bitácora, con la marca temporal del cierre. Útil para verificar que un día operativo quedó completo. |
| **Respuesta** | `[{bitacora_nombre, bitacora_codigo, total_registros, fecha_cierre}]`. |

---

### 4.6 Integración con el Dashboard de Generación

#### RF-050 — Generación automática de autorización

| | |
|---|---|
| **Descripción** | Cuando el JdT crea un registro en la bitácora Autorizaciones (código AUTH), el backend debe insertar automáticamente una fila asociada en `bitacora.autorizacion_dashboard`, extrayendo `periodo` y `valor_autorizado_mw` del JSON `campos_extra`. |
| **Actores** | Jefe de Turno. |
| **Postcondiciones** | Se inserta fila con `registro_origen_id` apuntando al registro de la bitácora, `planta_id`, `fecha=CAST(fecha_evento AS DATE)`, `periodo`, `valor_autorizado_mw`, `creado_por` = `sesion.usuario_id`, **`jdts_snapshot`** y **`jefes_snapshot`** (mismos snapshots que se generan para el registro de origen) y `activa=1`. |
| **Unicidad** | La combinación `(planta_id, fecha, periodo)` es única en `bitacora.autorizacion_dashboard`. Un intento de crear una autorización duplicada retorna HTTP 409 salvo que la existente esté con `activa=0`, en cuyo caso se reactiva actualizando el valor y refrescando los snapshots al momento de la reactivación. |

#### RF-051 — Exposición de autorizaciones al Dashboard

| | |
|---|---|
| **Descripción** | El sistema expone el endpoint `GET /api/autorizaciones?planta_id=&fecha=` que retorna todas las autorizaciones activas para esa planta y fecha. Este endpoint es consumido por el Dashboard de Generación. |
| **Actores** | Dashboard (cliente automático). |
| **Respuesta** | Arreglo de objetos: `[{autorizacion_id, planta_id, fecha, periodo, valor_autorizado_mw, jdts_snapshot, jefes_snapshot, creado_por_nombre, creado_en}]`. Los snapshots son JSON tal cual se persistieron. |
| **Reglas** | Solo se incluyen filas con `activa=1`. El orden es por `periodo` ascendente. |

#### RF-052 — Efecto de la autorización en el Dashboard

| | |
|---|---|
| **Descripción** | El Dashboard de Generación, al calcular la desviación de un periodo, debe consultar este endpoint y suprimir la alerta si existe una autorización activa para esa combinación planta/fecha/periodo. La lógica de supresión es responsabilidad del Dashboard. |
| **Notas** | El contrato entre ambos sistemas es únicamente la tabla `bitacora.autorizacion_dashboard` y el endpoint REST asociado. El Dashboard NO accede directamente al resto de tablas del módulo de Bitácoras. |

#### RF-053 — Anulación de autorización

| | |
|---|---|
| **Descripción** | Una autorización activa puede ser anulada mediante `DELETE /api/autorizaciones/:id`. La anulación es lógica (soft delete): se marca `activa=0`. |
| **Actores** | Jefe de Turno. |
| **Reglas** | El registro origen en la bitácora Autorizaciones no se elimina automáticamente; queda como evidencia de la autorización histórica. |

### 4.7 Disponibilidad de Plantas (mini-dashboard, F12+F13+F14)

La bitácora **DISP** se renderea como mini-dashboard interactivo (no como grilla genérica) y mantiene el estado vigente de cada planta. Los siguientes RF formalizan los endpoints y reglas que le aplican.

#### RF-054 — Vista del mini-dashboard

| | |
|---|---|
| **Descripción** | `GET /api/disponibilidad?planta_id=&historial_limit=&historial_offset=` devuelve el estado vigente de la planta y el historial paginado de cambios. Shape: `{ vigente, historial, historial_total }`. `vigente` puede ser `null` si la planta nunca tuvo registro (empty state UI). El mini-dashboard complementa esta vista con un panel de **acumulado histórico por estado** (D-028), alimentado por `GET /api/disponibilidad/metricas`: muestra el tiempo total en cada uno de los 4 estados, con el del estado vigente creciendo en vivo (lockstep con "Tiempo en estado") y los otros 3 congelados. |
| **Actores** | Cualquier usuario autenticado con `puede_ver=1` en DISP (todos los cargos). |
| **Parámetros** | `planta_id` requerido; `historial_limit` (default 20, max 100), `historial_offset` (default 0). |
| **Permiso** | `puede_ver` en DISP. |

#### RF-055 — Registro de cambio de disponibilidad

| | |
|---|---|
| **Descripción** | `POST /api/registros` (rama DISP, detectada por `bitacora.codigo='DISP'`) ejecuta un flujo transaccional con `UPDLOCK + HOLDLOCK`: valida que el evento sea distinto al vigente y que la fecha sea estrictamente posterior; cierra el vigente actual (UPDATE `fecha_fin_estado` + INSERT en histórico + DELETE del activo); inserta el nuevo en `registro_activo` con `fecha_fin_estado=NULL`; y hace UPSERT en `bitacora.disponibilidad_dashboard` (5.2 del MODBD). |
| **Actores** | Ingeniero Jefe de Turno o Ingeniero de Operación. |
| **Reglas** | Backend fuerza `tipo_evento_id = 'Cambio de Disponibilidad'` y `turno = NULL`. Multi-planta sin restricción (cualquier JdT/IngOp registra para cualquier planta). `detalle` es opcional. Errores: `409 mismo_estado`, `409 fecha_anterior_a_vigente`, `422` si fecha futura. |
| **Permiso** | `puede_crear` en DISP. |

#### RF-056 — Edición del registro vigente

| | |
|---|---|
| **Descripción** | `PUT /api/registros/:id` (rama DISP) edita `evento`, `fecha_inicio_estado` y `detalle` del registro vigente (no del histórico — RF rechaza con 422 si el `registro_id` está en histórico o tiene `fecha_fin_estado` no NULL). `planta_id` no es editable (422 si difiere del actual). Si cambia `evento`, valida contra el N-1 (último histórico): rechaza si `nuevo_evento == N-1.evento`. Si cambia `fecha_inicio_estado`, side-effect: actualiza `N-1.fecha_fin_estado = nueva_fecha` para mantener cronología sin gap. UPSERT final en `disponibilidad_dashboard` con `modificado_por` y `modificado_en`. |
| **Actores** | Cualquier usuario con `puede_crear` en DISP (no solo el creador). |
| **Permiso** | `puede_crear` en DISP. |

#### RF-057 — Deshacer último registro

| | |
|---|---|
| **Descripción** | `POST /api/disponibilidad/deshacer { planta_id }` borra el vigente y restaura el más reciente del histórico (lo despromueve a activo con `fecha_fin_estado=NULL`, lo elimina del histórico y hace UPSERT en `disponibilidad_dashboard`). Si no hay histórico, hace `DELETE` en `disponibilidad_dashboard` y la planta vuelve al empty state. En ambos casos emite un evento CIET tipo `'Deshacer disponibilidad'` con `campos_extra = { planta_id, evento_revertido, fecha_revertida, autor_delete: { usuario_id, nombre, cargo }, jdts_activos: [...], gerentes_activos: [...] }`. |
| **Actores** | Ingeniero Jefe de Turno o Ingeniero de Operación. |
| **Permiso** | `puede_crear` en DISP. |

#### RF-058 — Visibilidad universal con creación restringida

| | |
|---|---|
| **Descripción** | DISP es **visible para todos los cargos** (`puede_ver=1` universal en `cargo_bitacora_permiso`). Solo los cargos 1 (Jefe de Turno) y 2 (Ingeniero de Operación) tienen `puede_crear=1`. El frontend gatea los botones Cambiar/Editar/Deshacer en `EstadoActualCard` y el empty state CTA. El backend rechaza con HTTP 403 cualquier invocación de RF-055/056/057 sin permiso. |
| **Actores** | Todos los cargos (lectura); JdT e IngOp (escritura). |

#### RF-059 — Endpoint cross-app para Dashboard productivo

| | |
|---|---|
| **Descripción** | `GET /api/eventos-dashboard?tipo=DISP&planta_id=` se extiende para retornar el estado vigente desde `disponibilidad_dashboard` (no de `evento_dashboard`). Shape: `{ eventos: [{ planta_id, evento, codigo, fecha_inicio_estado, jdts_snapshot, jefes_snapshot, actualizado_en }] }`. Es el cimiento que F15 (futuro, fuera de este ciclo) usará para mostrar un badge de disponibilidad por planta en `dashboard-gen-gec3`. |
| **Actores** | Dashboard de Generación (cliente automático). |
| **Reglas** | Catch silencioso en el consumer (mismo patrón que F8 con AUTH/REDESP/PRUEBA): si bitácora cae, el dashboard sigue funcionando sin badge. |

### 4.8 Operación 24h con batch save y cierre automático (F16+F17+F18, reescrita por D-056 · ampliada por D-057 y D-058)

La bitácora **MAND** se renderiza como grilla 3×24 (3 tipos × 24 periodos × 2 plantas) con un único botón "Guardar". El cierre del día es automático y no requiere acción del usuario.

> **Desde D-056 (2026-07-22) la grilla es un formulario de captura append-only, no un espejo persistente.** Arranca **vacía**, cada Guardar **inserta registros nuevos e inmutables** agrupados en **lotes** (RF-060, RF-068), varios lotes pueden coexistir en el mismo periodo, y la grilla **no edita ni borra** nada de lo ya registrado. Debajo queda el **listado del día** (RF-070). Ya no se envía un diff contra el snapshot del último GET: el pivote que lo alimentaba se dio de baja.
>
> **Desde D-057 (2026-07-26) corregir y borrar existen — pero fuera de la grilla, por LOTE y desde el listado** (RF-071, RF-072). Capturar y corregir son dos planos separados: la grilla sigue naciendo vacía y sin cargar nada del servidor; la corrección vive en un modal que se abre desde el renglón del lote.
>
> **Desde D-058 (2026-07-27) el apartado además NARRA y EXPORTA.** Cada lote trae su **asiento** normalizado (RF-073) listo para copiar, se **refleja** como registro real en las dos bitácoras de Sala (RF-074) y el mes completo se descarga como el **libro GENE-F03** (RF-075). Los tres consumen el mismo motor server-side: el texto se redacta una sola vez y no puede divergir.

#### RF-060 — Captura append-only por lotes en la grilla MAND — reescrito (D-056, 2026-07-22)

| | |
|---|---|
| **Descripción** | `POST /api/sala-de-mando/guardar` con body `{ planta_id, fecha, filas: [{ tipo, hora, funcionariocnd, detalle, periodos: [{ periodo, valor_mw }] }] }` resuelve toda la batch dentro de una transacción única. **La grilla registra, no edita:** por cada fila válida el servidor genera un `lote_id` (GUID) y hace **un INSERT por celda con valor** — nunca UPDATE, nunca DELETE, nunca no-op. Las celdas vacías se omiten (ya no borran nada) y **pueden coexistir varios registros** para el mismo `(tipo, periodo, día, planta)`. Por cada celda tocada se recalcula el vigente publicado **por celda** (RF-069). Respuesta OK: `200 { resumen: { lotes, registros } }`. Validaciones de negocio acumulan errores y NO escriben (`400 { errores: [{tipo, periodo?, motivo}] }`); el frontend **conserva lo capturado** ante el 400 y solo vacía la grilla tras la confirmación. |
| **Actores** | Ingeniero Jefe de Turno, Ingeniero de Operación o Administrador y Debugging. |
| **Reglas** | `tipo ∈ {AUTH, PRUEBA, REDESP}`. `fecha` debe ser hoy en TZ Bogotá (sino `400 fecha_no_es_hoy`). La planta debe coincidir con la de la sesión (sino 403) — **sin allowlist de plantas en el endpoint** (D-055 (1)). Una fila con metadata pero **sin ninguna celda con valor** se rechaza con `lote_sin_celdas` (nunca un 200 mentiroso); una fila del todo vacía se ignora en silencio. Motivos: `fecha_no_es_hoy`, `tipo_invalido`, `periodos_invalido`, `periodo_fuera_rango`, `valor_mw_invalido`, `periodo_bloqueado`, `funcionariocnd_requerido`, `hora_requerida`, `hora_invalida`, `hora_futura`, `lote_sin_celdas`. **Retirados:** `detalle_sin_celdas` y el `modificado_por` selectivo (D-019) — en append-only nada se modifica al registrar; la regla vuelve con la corrección por lote (D-057). `turno_id` se estampa con `fechaOperativaDePeriodo`, **jamás** por `hora`. |
| **Permiso** | `puede_crear` en MAND. |

#### RF-068 — Hora de la llamada al CND y lote de captura (D-056, 2026-07-22)

| | |
|---|---|
| **Descripción** | Cada fila de la grilla lleva un campo **hora**, que es **la hora de la llamada con el CND** — distinta del momento de guardado. Se **precarga con la hora actual de Bogotá** y es **editable** (para registrar una llamada ocurrida antes), aplica a los **tres tipos** y es **obligatoria**. El conjunto de registros nacidos de la misma fila en el mismo Guardar es un **lote**: comparten `lote_id`, hora, funcionario y descripción. Persistencia en `campos_extra`: `lote_id` (GUID de 36 chars, generado por el **servidor**) y `hora_llamada` (**ISO UTC**, compuesta server-side desde el `HH:mm` Bogotá + la fecha de la grilla). `fecha_evento` **no cambia**: sigue siendo el instante de guardado. |
| **Actores** | Ingeniero Jefe de Turno, Ingeniero de Operación o Administrador y Debugging. |
| **Reglas** | La hora se valida **a nivel de fila** (el error viaja **sin `periodo`**) contra el reloj del **servidor**, nunca el del cliente, con **5 minutos de tolerancia**: falta o no es `HH:mm` → `hora_requerida`/`hora_invalida`; fuera del día Bogotá de la grilla → `hora_invalida`; posterior a "ahora"+5 min → `hora_futura`. El lock de REDESP (RF-063 / RN-14) protege el **valor**, nunca la hora ni la descripción. En los registros previos a la migración `F32.A1` la clave `hora_llamada` queda **AUSENTE** (ni `null` ni vacío: no se adivina un dato que no existía) y esos registros van **últimos** en todo ordenamiento por hora. La metadata del lote queda **replicada en cada celda** sin constraint que la mantenga coherente — la sostiene un guard de test. Disponibilidad **no** lleva este campo. Ver **D-056** y BIT-MODBD §7.9. |
| **Permiso** | `puede_crear` en MAND. |

#### RF-069 — Publicación al dashboard resuelta por celda (D-056, 2026-07-22)

| | |
|---|---|
| **Descripción** | Con varios registros posibles por periodo, la fila publicada en `bitacora.evento_dashboard` deja de resolverse por upsert y pasa a **recalcularse desde cero por CELDA** — `(planta_id, fecha, periodo, tipo)` — dentro de la misma transacción del guardado (`recalcularEventoDashboard`, `server/utils/notificador.js`). Gana el registro vivo con **mayor hora de llamada**; empate → el creado más recientemente. Si tras la operación **no queda ningún registro** para esa celda, la fila de `evento_dashboard` se **BORRA** (no se desactiva): el origen dejó de existir y el puntero quedaría huérfano. La notificación al dashboard (`notifyDashboard`) sigue siendo post-commit y fire-and-forget. |
| **Actores** | Sistema (efecto del guardado). |
| **Reglas** | La decisión es **por celda, nunca por lote**: dos lotes pueden solaparse **parcialmente** (A cubre P14–P18 a las 09:12, B cubre P16–P20 a las 09:40 → P14–P15 publican A, P16–P20 publican B). Los registros **sin `hora_llamada`** van **últimos** y **nunca ganan por hora**: caen al desempate por `creado_en`/`registro_id`. Se **conserva** el UNIQUE `UQ_evento_planta_fecha_periodo_tipo`: el dashboard sigue viendo un solo valor por periodo y tipo, y el contrato cross-repo no cambia de shape. El **soft-delete (`activa=0`) sigue vigente en el cierre diario** (RF-061), donde el origen no desaparece sino que migra al histórico. `upsertEventoDashboard` permanece intacto para la rama genérica de `/api/registros`. Ver **D-056** y BIT-MODBD §5.1. |

#### RF-070 — Listado del día bajo la grilla, solo lectura (D-056, 2026-07-22)

| | |
|---|---|
| **Descripción** | `GET /api/sala-de-mando/lotes?planta_id=&fecha=` (`fecha` opcional, default hoy Bogotá) devuelve **los lotes del día** de la planta, agrupados por `lote_id`, con su tipo, hora de llamada, funcionario, descripción, autor, los periodos con su valor en MW y la marca `publicado` **por celda**. El frontend lo pinta debajo de la grilla (`LotesDelDia.jsx`) sin filtros y **sin temporizador propio** — se refresca al montar, tras cada guardado exitoso y en el mismo tick de 60s de la grilla. Reemplaza funcionalmente al pivote `GET /api/sala-de-mando`, **dado de baja** (404): su shape "un valor por celda + metadata única de fila" dejó de tener sentido. **El endpoint sigue siendo de solo lectura**; desde D-057 el renglón suma las acciones de corregir y borrar (RF-071, RF-072), que son endpoints propios. Desde **D-058** cada lote viaja además con su campo **`asiento`** —el texto normalizado ya armado por el servidor (RF-073)— que el renglón pinta a todo el ancho, con **copiar renglón** y **copiar el día completo** (REQ-04 §8.3). Copiar **no es escribir**: los dos botones aparecen aunque el cargo no tenga `puede_crear`. |
| **Actores** | Cualquier cargo con `puede_ver` en MAND. |
| **Reglas** | Exige `puede_ver` explícitamente; **no** exige `puede_crear` (RN-04.f) — quien no escribe igual consulta, y en ese caso el listado se pinta **sin ningún control**. Fuente: **solo `registro_activo`** (el día en curso); lo del día anterior lo congela el cierre automático (RF-061) y se consulta en Históricos. La metadata se deriva **asumiendo coherencia** del lote, sin el desempate ad-hoc "el primero gana" del pivote viejo: si diverge, que se note. `publicado` es un **indicador derivado, no un control** (no filtra ni habilita acciones). Orden: `hora_llamada DESC` con los sin-hora al final, desempate `creado_en DESC`. Los registros migrados por `F32.A1` aparecen como lotes de un solo periodo con hora vacía. Un lote de periodos **no contiguos** se muestra como lista de periodos, no como rango (REQ-04 §8.4: los lotes no contiguos se permiten). |

#### RF-071 — Corrección por lote desde el listado (D-057, 2026-07-26)

| | |
|---|---|
| **Descripción** | `PUT /api/sala-de-mando/lotes/:lote_id` con body `{ planta_id, hora, detalle, funcionariocnd, periodos: [{ periodo, valor_mw }] }` corrige **el lote completo** en una sola transacción: MW, hora de la llamada, funcionario, descripción **y el conjunto de periodos que abarca** (REQ-04 §3.3). Es un **diff quirúrgico**, no un reemplazo: las celdas que sobreviven conservan `registro_id`, `creado_por` y `creado_en`; se `UPDATE`an solo las que cambian de valor, se `DELETE`an las que ya no vienen y se `INSERT`an las nuevas con el **mismo `lote_id`**. El **tipo no se acepta en el body**: es inmutable (corregirlo es borrar y volver a registrar). Cada celda tocada recalcula su publicación (RF-069) y, si cambió la hora, se recalculan **todas** las del lote —la hora es el criterio de desempate—. Respuesta OK: `200 { lote_id, resumen: { actualizados, creados, eliminados, celdas_recalculadas } }`. |
| **Actores** | Ingeniero Jefe de Turno, Ingeniero de Operación o Administrador y Debugging — **indistintamente, sea o no el autor** (RQ-04.11). |
| **Reglas** | Gate: `puede_crear` en MAND + planta de la sesión. **Excepción acotada a D-049**, que **no** vive en `canEditarRegistro` (MAND no pasa por ese helper): vive en que el gate es el permiso de crear y no `creado_por` — `permissions.js` no se toca y el resto de bitácoras conserva "solo el autor" intacto. Se revalida todo lo del `POST` (hora `HH:mm` Bogotá contra la **fecha del lote** con 5 min de tolerancia, `funcionariocnd` obligatorio en AUTH y forzado a `NULL` en PRUEBA/REDESP, `periodo` 1..24, `valor_mw` finito). El **lock de REDESP actúa sobre el DELTA**: rebota `periodo_bloqueado` si un periodo pasado cambia de valor, se agrega o se quita; deja pasar hora, funcionario, descripción y los periodos pasados idénticos (RN-14). `valor_mw` ausente **borra** la celda (no es error); el mismo periodo dos veces → `periodo_duplicado`; quedarse sin ninguna celda → `lote_sin_celdas` (nunca un 200 mentiroso, RN-04.b). El diff se calcula **dentro de la transacción** contra la BD, no contra el snapshot del modal. `fecha_evento` se **hereda** del lote en las celdas insertadas (el lote no se parte entre dos días Bogotá) y su `turno_id` sale de `fechaOperativaDePeriodo`. **Auditoría (RN-04.d):** cualquier cambio —valor, hora, funcionario o descripción— sella `modificado_por`/`modificado_en` en las celdas afectadas; el `modificado_por` selectivo de D-019 rige solo en la captura. Desenlaces: `404 lote_inexistente` · `409 lote_cerrado` (ya archivado por el cierre diario, RF-032 intacto) · `403 lote_de_otra_planta`. Disponible **aunque el turno esté finalizado o cerrado** (RF-067 / RF-065: MAND exenta). Ver **D-057**. |
| **Permiso** | `puede_crear` en MAND. |

#### RF-072 — Borrado real por lote (D-057, 2026-07-26)

| | |
|---|---|
| **Descripción** | `DELETE /api/sala-de-mando/lotes/:lote_id?planta_id=` borra **realmente** las N filas del lote (RN-04.c: en MAND no hay anulación visible — un renglón tachado en la lista de llamadas al CND confunde en vez de informar) y recalcula **cada** celda `(periodo, tipo)` que ocupaba: lo publicado **retrocede al lote anterior vigente** de ese periodo, o la fila de `evento_dashboard` se **elimina** si no queda ninguno (RF-069). Todo en una transacción; `notifyDashboard` post-commit. Respuesta OK: `200 { lote_id, resumen: { eliminados, celdas_recalculadas } }`. El frontend lo confirma con un modal que muestra el lote completo y **advierte el retroceso de lo publicado**. |
| **Actores** | Los mismos de RF-071, sea o no el autor. |
| **Reglas** | Mismo gate y mismos cuatro desenlaces que RF-071 (`404 lote_inexistente` / `409 lote_cerrado` / `403 lote_de_otra_planta`). **El lock de REDESP NO aplica al borrado del lote completo**: el lock protege el valor de un periodo despachado contra una reescritura silenciosa, pero borrar es corregir un registro **errado** — si aplicara, un redespacho mal digitado quedaría publicado para siempre en cuanto pasara su hora. `planta_id` viaja por **query string** (un `DELETE` no lleva body fiable). Ver **D-057**. |
| **Permiso** | `puede_crear` en MAND. |

#### RF-073 — Asiento normalizado del evento de operación (D-058, 2026-07-27)

| | |
|---|---|
| **Descripción** | El texto con que se narra un evento lo **arma el servidor**, una sola vez, en un módulo puro (`server/utils/asientos/`), y alimenta a los tres consumidores: el renglón del listado del día (RF-070), la copia a las bitácoras de Sala (RF-074) y el libro mensual (RF-075). Plantillas: **AUTH** `Se recibe llamada del CND ({funcionariocnd}) autorizando {unidad} a generar {carga}` · **REDESP** `Se recibe del CND redespacho para {unidad}: {carga}` · **PRUEBA** `Se declara prueba de {unidad} a {carga}` · **DISP** una frase por estado (`E/L en servicio` · `disponible en reserva, sin generar` · `F/L indisponible` · `F/L en mantenimiento programado`). Lo escrito a mano en las bitácoras de Sala pasa **literal**, sin normalizar ni corregir, con prefijo `{UNIDAD} — ` **solo si el texto no nombra ya la unidad**. |
| **Actores** | Ninguno directo — es el motor que sirve a RF-070, RF-074 y RF-075. |
| **Reglas** | Convenciones canónicas: unidad `GEC3`/`GEC32` (nunca `G3.0`/`G3.2`), potencia entera con **`MW`** (es potencia por periodo, **no `MWh`**), periodos **compactados** a `del P17 al P19` cuando todas las celdas del lote comparten valor y desplegados a `P17: 109 MW; P18: 134 MW` cuando difieren, `detalle` al final tras punto y punto de cierre siempre. **La hora NUNCA va dentro del asiento** (es la columna `HH:MM` del formato y una columna propia del listado). **Sin verbo de sentido en AUTH** (el sistema conoce el valor autorizado, no el vigente contra el cual compararlo). El módulo es **puro**: sin BD, sin reloj, sin red — y **el frontend no conoce ninguna plantilla**. Un tipo o estado desconocido **lanza** donde el texto se persiste (RF-074) y **degrada a "sin renglón" + log** donde solo se muestra (RF-070, RF-075): un dato raro no puede tumbar la jornada ni el mes. Ver **D-058** y `FORMATO-ASIENTOS-OPERACION.md`. |
| **Permiso** | N/A. |

#### RF-074 — Reflejo del lote de Operación 24h a las bitácoras de Sala (D-058, 2026-07-27)

| | |
|---|---|
| **Descripción** | Al registrar un lote en Operación 24h, su asiento (RF-073) se **copia como registro real** de `SALAJDT` **y** `SALAING` —las dos siempre, sin importar quién lo originó (RQ-02.2)—, dentro de la **misma transacción** del `POST /guardar`. Corregir el lote (RF-071) **regenera** el texto de las dos copias; borrarlo (RF-072) las **borra**. `SALAOP` nunca recibe copia (RQ-02.3). La copia es un registro como cualquier otro: cuenta en el contador de la pestaña, entra al cierre de turno de esa bitácora y viaja al histórico (RQ-02.4). |
| **Actores** | Ninguno directo: lo dispara el registro/corrección/borrado del lote en MAND. El **autor de la copia es el autor del origen** (RN-02.c). |
| **Reglas** | **Atómico con el origen** (RQ-02.9): si el reflejo falla, se revierte también el lote — sin `try/catch`. Vínculo `campos_extra.origen_lote_id` (+ `origen_bitacora`), **por lote y nunca por `registro_id`** (la copia también migra al histórico → no hay FK posible). `fecha_evento` = **hora de la llamada** (narrativo) pero `turno_id` = el turno **ABIERTO** de la unidad (puntero de archivado, RF-066/D-045), o `NULL` si no hay ninguno: apuntarla a un turno ya cerrado la dejaría viva en `registro_activo` para siempre. La cascada alcanza **solo las copias vivas**: `rowsAffected = 0` **no es error** (ya se archivaron), el histórico no se reescribe (RF-032) y la corrección del origen **procede igual** — un `409` volvería incorregible un lote a las 18:01 por el estado de su reflejo. **Solo lectura en su destino, también para su autor**: `PUT`/`DELETE /api/registros/:id` responden `403 asiento_reflejado` y el `GET /activos` la marca con `puede_editar = false` (restricción, **no** un bypass de RF-049/D-049). No publica al dashboard (RN-02.a) ni cuenta para presencia o conformación (RN-02.b); **no hay retroactividad** (RQ-02.13) y `TEST_PLANTA` no refleja (RN-02.e). El **reflejo de Disponibilidad queda fuera** (sus tipos espejo ya están sembrados): ADR propio pendiente. Ver **D-058**. |
| **Permiso** | El del origen (`puede_crear` en MAND). No se teclea a mano: los tipos espejo tienen `seleccionable = 0`. |

#### RF-075 — Libro mensual GENE-F03 descargable (D-058, 2026-07-27)

| | |
|---|---|
| **Descripción** | `GET /api/sala-de-mando/reporte-mensual?mes=YYYY-MM` devuelve **un solo `.xlsx`** con **una hoja por día** del mes y **las dos unidades mezcladas** en orden cronológico, calcado del formato controlado **GENE-F03**: encabezado con logo y datos del formato, y **tres bloques de turno** (`00:00-06:00` = cola del T2 de ayer · `06:00 - 18:00` = T1 · `18:00 - 00:00` = cabeza del T2) con `JEFE DE TURNO` / `INGENIERO DE TURNO` y la tabla `HH:MM | DESCRIPCIÓN EVENTO Y/O ACTIVIDAD`. Se descarga desde una barra propia en Operación 24h (`<input type="month">` + botón), con el mes en el hash (`#/op24h?mes=YYYY-MM`). El libro clona una **plantilla real** derivada del F03 (`server/assets/f03-plantilla.xlsx`) — **cero dependencias nuevas**. |
| **Actores** | Los cargos con `puede_crear` en MAND. |
| **Reglas** | **Solo lectura**: no escribe una sola fila. Gate `puede_crear` **y no `puede_ver`** — la matriz le da `puede_ver` en MAND a todos los cargos, así que exigirlo sería no exigir nada (el mismo cargo que recibe `403` acá sigue consultando el listado: consultar no es descargar). `mes` es **opcional** (sin él, el mes Bogotá en curso); formato inválido → `400 mes_invalido`, mes futuro → `400 mes_futuro`, mes **sin eventos no es error** (libro con hojas vacías). Fuentes: MAND (`registro_activo` ∪ `registro_historico`, agrupado por lote), DISP (por `fecha_inicio_estado`) y las dos bitácoras de Sala **excluyendo los asientos reflejados** (o el evento saldría tres veces). Cada evento se ubica por su **hora canónica** —MAND por `hora_llamada`, **nunca** por `fecha_evento`; ausente en los migrados → derivada del primer periodo— y cae en su bloque **por hora de calendario, no por `turno_id`**: el **T2 se parte por medianoche** y cada evento aparece **exactamente una vez** en todo el libro. Orden **ascendente** dentro del bloque (deliberadamente inverso al del listado, RN-04.a). Alcance: solo las unidades reales (`GEC3`, `GEC32`) — las plantas-fixture **nunca** se exportan (RN-06.g). El encabezado une `conformacion_turno` y `turno_participante`, excluye usuarios sintéticos y **queda en blanco** si nadie de esos dos cargos estuvo: el papel también se deja vacío. Ver **D-058** y REQ-06. |
| **Permiso** | `puede_crear` en MAND. |

#### RF-061 — Cierre automático al fin de día

| | |
|---|---|
| **Descripción** | Sweeper interno (`server/utils/mand-sweeper.js`) corre cada 60s, detecta cambio de día Bogotá y cierra el día anterior para ambas plantas (GEC3, GEC32). Mueve los registros borrador a `registro_historico` con `estado='cerrado'`, soft-deletea las filas en `evento_dashboard` (`activa=0`) y emite CIET con autor SISTEMA. Resiliente a reinicios: el primer tick post-arranque intenta cerrar AYER por si el server estaba caído al cruzar medianoche. |
| **Actores** | Sistema (proceso interno). |
| **Reglas** | Idempotente vía `bitacora.mand_cierre_log` (PK `(fecha_cerrada, planta_id)`). Si el día no tuvo registros, el sweeper hace early return SIN emitir CIET ni escribir en el log. `POST /api/sala-de-mando/cierre-diario` (permiso `puede_cerrar_turno`) dispara el mismo helper para tests/recovery operativo. |

#### RF-062 — Multi-select Excel-like para replicar valor

| | |
|---|---|
| **Descripción** | El operador puede seleccionar múltiples celdas de un mismo tipo (Shift-click rango contiguo, Ctrl/Meta-click toggle individual, drag con `onMouseEnter`). Escribir un valor en cualquiera de las seleccionadas y apretar Enter replica ese valor a toda la selección en el buffer en memoria. Esc o clic fuera de la tabla limpia la selección. Cross-tipo prohibido — clickear otra fila descarta la selección anterior. |
| **Actores** | Ingeniero Jefe de Turno o Ingeniero de Operación. |
| **Reglas** | El multi-select solo afecta el buffer; persiste al backend recién con "Guardar". Visual: `outline 2px solid <color del tipo>` + fondo sutil en celdas seleccionadas. |

#### RF-063 — Lock REDESP por hora actual

| | |
|---|---|
| **Descripción** | Las celdas REDESP con `periodo < periodoActual` (= `floor(horaBogota) + 1`) aparecen `disabled` en el frontend con tooltip explicativo. Auto-update del lock cada 60s. El backend rechaza con `400 periodo_bloqueado` cualquier intento de POST que cruce la frontera entre Guardar y procesar (defensa en profundidad si la hora cambió justo entre el render y el send, o si el cliente fue manipulado). |
| **Actores** | Ingeniero Jefe de Turno o Ingeniero de Operación. |
| **Reglas** | Solo aplica a `tipo='REDESP'`. AUTH y PRUEBA pueden registrarse a-posteriori dentro del día. El check de bloqueo solo se ejecuta cuando `valor_mw != null`. **D-056:** la grilla ya no carga valores existentes (nace vacía), así que el `disabled` solo impide capturar; y el lock protege **el valor**, nunca la **hora** de la llamada ni la descripción — una llamada de hace 45 minutos se registra igual (RN-03.c). |

#### RF-064 — FuncionarioCND condicional por tipo

| | |
|---|---|
| **Descripción** | El campo `funcionariocnd` es requerido en filas AUTH cuando hay al menos un periodo con `valor_mw != null` (validación front + back, motivo `funcionariocnd_requerido`). Para filas PRUEBA y REDESP, el input aparece deshabilitado con placeholder "No aplica" y el server fuerza `funcionariocnd = NULL` en persistencia (silencioso). **D-056: sin cambios** — la regla se evalúa ahora por **lote** (la fila de ese Guardar) en vez de por la fila del día. |
| **Actores** | Ingeniero Jefe de Turno o Ingeniero de Operación. |
| **Reglas** | El frontend muestra borde ámbar en el input AUTH cuando hay valor en algún periodo y `funcionariocnd` está vacío (warn proactivo). El `funcionariocnd_requerido` se reporta a nivel fila, no a nivel celda — un único error AUTH agrupado en el array `errores`. |

#### RF-065 — Exclusión de MAND y DISP del cierre de turno — actualizado (D-042, 2026-07-03)

| | |
|---|---|
| **Descripción** | MAND y DISP no se cierran por el cierre de turno: quedan fuera del cierre de turno masivo (`POST /api/cierre/masivo`) vía `AND b.codigo NOT IN ('DISP','MAND')` (desde F10, confirmado en F16). MAND se cierra exclusivamente vía sweeper diario (RF-061); DISP archiva al llegar un nuevo estado. Con la eliminación del cierre individual (D-042) ya no existe `POST /api/cierre/bitacora` ni sus códigos de rechazo `400 mand_cierre_individual_no_permitido` / `422 bitacora_no_cerrable` — no hay endpoint que los devuelva. El frontend oculta el botón de cierre en la pestaña MAND (el único botón de acción del header en MAND es "Guardar"). |
| **Actores** | N/A (exclusión estructural, no una acción del usuario). |
| **Reglas** | La exclusión `codigo NOT IN ('DISP','MAND')` en el cierre de turno es la única regla vigente. Los antiguos códigos de rechazo del cierre individual quedaron obsoletos al eliminarse el endpoint. |

#### RF-066 — Conformación de turno (snapshot histórico)

| | |
|---|---|
| **Descripción** | Al cierre de cada turno (T1/T2 por planta GEC3/GEC32) se escribe snapshot inmutable a `bitacora.conformacion_turno` con usuario_id, usuario_nombre, cargo_id, cargo_nombre, inicio_sesion, fin_sesion, duracion_min y bit `fin_inferido`. Trigger híbrido: `turno-sweeper.js` extendido (tras finalizar `sesion_bitacora` por agotamiento) + catchup en `initDB()` al arranque cubriendo los últimos 7 días Bogotá. Endpoints: `GET /api/conformacion-turno?fecha=&turno=&planta_id=` (consulta abierta a cualquier rol con sesión); `POST /api/conformacion-turno/trigger` (admin: dispara manual con body `{fecha_operativa, planta_id, turno}`, soporta `?force=true` para bypass de ventana en curso). |
| **Actores** | Lectura: cualquier rol con sesión válida. Trigger admin: Ingeniero Jefe de Turno, Ingeniero de Operación o Jefe de Planta (`puede_cerrar_turno=1` o `es_jefe_planta=1`). |
| **Reglas** | Una sesión cuenta para el turno X si su `inicio_sesion` cae dentro de la ventana de X — D-003 fija el turno al login. Logout explícito pobla `sesion_activa.cerrada_en` (fix retro F2); si NULL al snapshot, builder usa `ventanaTurno().fin` con `fin_inferido=1`. Idempotencia natural vía PK `(fecha_operativa, planta_id, turno, usuario_id)`. Ver D-025 y BIT-MODBD §4.7. |

---

#### RF-067 — Finalizar / revertir turno + inhibición de registro en bitácoras generales

| | |
|---|---|
| **Descripción** | Todo ingeniero logueado puede declarar que **terminó su turno** (`POST /api/bitacora/finalizar`) y **revertir** esa decisión libremente si se equivocó (`POST /api/bitacora/revertir-turno`, self-service, sin permiso especial). El estado es la columna **`sesion_activa.turno_finalizado_en`** (fuente única, NULL = turno vivo). Mientras el turno esté finalizado, el sistema **inhibe crear/editar/borrar** en las bitácoras **generales** (respuesta `409 turno_finalizado`); MAND, DISP y COMB siguen operables (endpoints propios). El JdT/IngOp también puede finalizar en nombre de otros (`finalizar-forzado`). Cada finalizar/revertir emite un CIET (`Finalización de turno` / `Reapertura de turno`), idempotente. |
| **Actores** | Finalizar/revertir la propia: cualquier rol con sesión válida. Forzar: Ingeniero Jefe de Turno / Ingeniero de Operación (`puede_cerrar_turno=1`). |
| **Reglas** | `sesion_bitacora.finalizada_en` es SOLO presencia por-bitácora — **ver** una bitácora ya **no** des-finaliza el turno (bug corregido). El listado "ingeniero no finalizado" del cierre del JdT (`GET /api/cierre/preview-masivo`) filtra por `sa.turno_finalizado_en IS NULL`. El estado muere solo (sweeper → `activa=0`; `select-context` lo resetea al reactivar). El front lo deriva de `/api/me` (sin `localStorage`) y sobrevive F5; el botón togglea a "Revertir finalización" + banner. Ver **D-040** y BIT-MODBD §3 / §4.6. |

---

## 5. Requerimientos no funcionales

### 5.1 Rendimiento

- Los endpoints de lectura deben responder en menos de **500 ms** bajo carga esperada (10 usuarios concurrentes).
- El endpoint de cierre de turno (RF-031) debe completar cada transacción por bitácora en menos de **2 segundos** para bitácoras con hasta 100 registros activos.
- La vista `bitacora.v_historico_busqueda` debe soportar consultas paginadas sobre al menos **100.000 registros históricos** sin degradación.
- El intento de `POST /api/auth/resume` durante el reload debe completarse en menos de **300 ms** para no impactar la percepción de carga inicial.

### 5.2 Seguridad

- Todos los endpoints de escritura deben validar que la sesión del solicitante esté activa (TTL vigente) y que el cargo tenga los permisos requeridos.
- Las consultas a base de datos deben usar únicamente parámetros (`.input()` de `mssql`), nunca concatenación de cadenas, para prevenir inyección SQL.
- Las contraseñas deben almacenarse hasheadas en producción. Durante desarrollo se acepta la contraseña literal `'1234'` para todos los usuarios (marcado como deuda técnica para migración a Keycloak).
- La comunicación con el Dashboard puede permanecer en HTTP en red interna; en exposición externa debe protegerse con HTTPS.
- El header `X-Sesion-Id` es obligatorio en todo endpoint no marcado `skipAuth`. Su ausencia o invalidez devuelve 401.

### 5.3 Usabilidad

- El flujo de login → selección de contexto → primera bitácora debe requerir máximo 5 clics.
- Los tres campos manuales (fecha/hora, turno, detalle) deben ser los únicos editables en la creación de un registro general. Los campos automáticos (snapshots) se presentan en modo visualización, nunca como inputs.
- Las bitácoras especiales deben exponer su formulario adicional con etiquetas en español y ayuda inline.
- El frontend debe mantener coherencia visual con el prototipo aprobado (Tailwind, Lucide icons, tema claro).
- **Al recargar la página (F5), el usuario no debe perder su sesión ni su contexto operativo, siempre que el reload ocurra dentro del TTL de 5 minutos.** La transición debe ser imperceptible más allá de un breve "Cargando…".

### 5.4 Disponibilidad y tolerancia a fallos

- Si el backend o la base de datos no están disponibles, el frontend debe presentar un mensaje claro sin crashear y permitir reintento manual.
- Los registros en estado `'borrador'` deben persistir en el backend tras cada guardado individual para evitar pérdida por recarga accidental del navegador.
- La transacción de cierre (RF-030) debe ser atómica: nunca debe dejar registros huérfanos entre `registro_activo` y `registro_historico`.
- La ventana entre `pagehide` (que marca la sesión `activa=0`) y el subsiguiente `resume` debe ser tolerante: bajo `React.StrictMode` los efectos se montan, limpian y re-montan; ningún otro request autenticado debe salir durante ese interludio.

### 5.5 Mantenibilidad y escalabilidad

- Añadir una nueva bitácora debe requerir únicamente `INSERT` en `lov_bit.bitacora` y, si aplica, `INSERTs` en `lov_bit.tipo_evento` y `lov_bit.cargo_bitacora_permiso`. No debe modificarse código del backend ni del frontend.
- La adición de nuevos cargos sigue el mismo principio: `INSERT` en `lov_bit.cargo` y configuración de permisos.
- El crecimiento de registros históricos debe manejarse mediante particionamiento por `fecha_cierre_operativo` cuando el volumen supere 1 millón de filas (fuera del alcance de esta versión, pero el índice `IX_rh_fecha` está preparado).
- Los snapshots evitan la degradación por FKs rotas: eliminar o desactivar un usuario antiguo no afecta los registros históricos que lo referencian.

### 5.6 Auditoría

- Todo registro lleva cuatro campos obligatorios de auditoría: `creado_por` (FK a `lov_bit.usuario`), `creado_en`, `modificado_por`, `modificado_en`.
- Todo registro histórico lleva además `cerrado_por`, `cerrado_en` y `fecha_cierre_operativo`.
- Todo registro (activo e histórico) y toda autorización llevan los snapshots inmutables `ingenieros_snapshot` / `jdts_snapshot` / `jefes_snapshot` con los nombres y `usuario_id` vigentes al momento de su creación.

---

## 6. Matriz de trazabilidad RF ↔ Modelo de datos

Esta matriz permite verificar la cobertura del modelo de datos para cada requerimiento funcional. Toda tabla referenciada se define formalmente en BIT-MODBD-2026-001.

| RF | Descripción breve | Tablas y campos involucrados |
|---|---|---|
| RF-001 | Inicio de sesión | `lov_bit.usuario` (`email`, `password_hash`, `activo`) |
| RF-002 | Selección de contexto | `bitacora.sesion_activa` (todos los campos, `ultima_actividad=GETDATE()`) |
| RF-003 | Heartbeat | `bitacora.sesion_activa.ultima_actividad` |
| RF-004 | Logout explícito | `bitacora.sesion_activa.activa = 0` |
| **RF-005** | **Logout vía `sendBeacon` (`pagehide`)** | **`bitacora.sesion_activa.activa = 0`** |
| **RF-006** | **Reanudación post-reload (`resume`)** | **`bitacora.sesion_activa` (`activa=1`, `ultima_actividad=GETDATE()` si dentro de TTL)** |
| **RF-007** | **TTL enforcement en backend** | **Predicado `activa=1 AND ultima_actividad > DATEADD(MINUTE,-5,GETDATE())` + barrido en `initDB()`** |
| RF-010 | Listado filtrado por cargo | `lov_bit.cargo_bitacora_permiso`, `lov_bit.bitacora` |
| RF-011 | Catálogo dinámico | `lov_bit.bitacora` (`definicion_campos`, `orden`, `activa`) |
| RF-012 | Tipos de evento | `lov_bit.tipo_evento` (`bitacora_id`, `es_default`, `orden`) |
| RF-020 | Creación registro general | `bitacora.registro_activo` (`creado_por`, **`ingenieros_snapshot`**, **`jdts_snapshot`**, **`jefes_snapshot`**, auditoría) |
| RF-021 | Registro en bitácora especial | `bitacora.registro_activo.campos_extra` + snapshots; `lov_bit.bitacora.definicion_campos` |
| RF-022 | Edición de borrador | `bitacora.registro_activo` (`modificado_por`, `modificado_en`, `estado`). **Snapshots inmutables.** |
| RF-023 | Eliminación de borrador | `bitacora.registro_activo`; `bitacora.autorizacion_dashboard.activa=0` (si AUTH) |
| RF-024 | Listado de activos | `bitacora.registro_activo` + snapshots JSON + JOIN a `lov_bit.usuario` para `creado_por_nombre` + JOIN a `tipo_evento` |
| ~~RF-030~~ | ~~Cierre individual~~ — RETIRADO (D-042) | — (eliminado; único cierre = RF-031) |
| RF-031 | Cierre de turno | `bitacora.registro_activo` → `bitacora.registro_historico` (copia snapshots + `cerrado_por`, `cerrado_en`, `fecha_cierre_operativo`), en bucle de transacciones por `bitacora_id`; `POST /api/cierre/masivo` |
| RF-032 | Inmutabilidad histórica | `bitacora.registro_historico` (regla funcional, no DDL) |
| RF-040 | Búsqueda histórica | `bitacora.v_historico_busqueda` (vista con snapshots ya formateados, sin JOIN de roles) |
| RF-041 | Detalle histórico | `bitacora.registro_historico` + snapshots JSON |
| RF-042 | Resumen diario | `bitacora.registro_historico` agregado por `bitacora_id` y `fecha_cierre_operativo` |
| RF-050 | Autorización automática | `bitacora.autorizacion_dashboard` (insertado al crear en AUTH, con `creado_por`, `jdts_snapshot`, `jefes_snapshot`) |
| RF-051 | Endpoint al Dashboard | `bitacora.autorizacion_dashboard` (filtrado `activa=1`, expone snapshots JSON) |
| RF-052 | Supresión de desviación | Responsabilidad del Dashboard; contrato = tabla anterior |
| RF-053 | Anulación | `bitacora.autorizacion_dashboard.activa=0` (soft delete) |

---

## 7. Reglas de negocio críticas

Las siguientes reglas deben respetarse en toda implementación, pruebas y mantenimiento del sistema. No son negociables sin aprobación explícita del área usuaria.

### RN-01 — Exclusividad del cierre

Solo un usuario con cargo 'Jefe de Turno' y sesión activa (dentro del TTL) puede ejecutar el cierre de una bitácora. Esta regla se valida en el backend consultando el cargo de la sesión asociada al usuario solicitante.

### RN-02 — Snapshot de JdTs congelado al momento del registro

Al insertar un registro, el campo `jdts_snapshot` se calcula consultando `bitacora.sesion_activa` con filtro TTL y se persiste como JSON inmutable. Este valor queda congelado: si el JdT cambia posteriormente, los registros ya creados conservan la lista original. Si no hay JdTs activos al momento, se persiste el snapshot con el usuario `es_jdt_default=1`. Esto preserva la trazabilidad histórica exacta sin depender de FKs que podrían corromperse ante cambios posteriores.

### RN-03 — Snapshot de jefe de planta

El campo `jefes_snapshot` se calcula leyendo todos los usuarios con `es_jefe_planta=1` al momento del registro y se persiste como JSON. En la semilla actual es un singleton (Ernesto Muñoz), pero el modelo soporta múltiples. Si este rol cambia en el futuro, el nuevo titular debe marcarse explícitamente con `es_jefe_planta=1` y el anterior con 0, en una misma transacción. **Los registros previos conservan el snapshot con el titular anterior — la historia no se reescribe.**

### RN-04 — Unicidad de autorización por periodo

Para una combinación `(planta_id, fecha, periodo)` solo puede existir una autorización activa. La restricción `UQ_auth_planta_fecha_periodo` en `bitacora.autorizacion_dashboard` garantiza esto a nivel de base de datos. Un intento de crear un duplicado debe manejarse en capa de aplicación: si la existente tiene `activa=0`, se reactiva con el nuevo valor y se refrescan los snapshots; si `activa=1`, se rechaza con HTTP 409.

### RN-05 — Atomicidad del cierre

La secuencia `INSERT→DELETE` del cierre se ejecuta dentro de una transacción SQL. Bajo ninguna circunstancia debe existir un registro en `registro_historico` sin su contraparte eliminada en `registro_activo`, ni viceversa. Dentro de transacciones `mssql` las queries se serializan con `await` — prohibido `Promise.all`.

### RN-06 — Turno calculado, no almacenado estáticamente

El turno de una sesión se calcula al momento de su creación basado en la hora Colombia. Si el usuario inicia sesión a las 11:59 y permanece hasta las 12:30, la sesión conserva `turno=1`. Un nuevo registro en `turno=2` requiere cerrar sesión y volver a seleccionar contexto.

### RN-07 — Campos JSON validados en aplicación

El contenido del campo `campos_extra` se valida exclusivamente en la capa de aplicación contra el esquema de `lov_bit.bitacora.definicion_campos`. La base de datos solo verifica que sea un `NVARCHAR(MAX)` válido. Los snapshots (`ingenieros_snapshot`, `jdts_snapshot`, `jefes_snapshot`) son generados y validados por el backend, nunca por el cliente. Nunca confiar en datos no validados provenientes del cliente.

### RN-08 — Desacoplamiento con el Dashboard

El único contrato entre el módulo de Bitácoras y el Dashboard de Generación es la tabla `bitacora.autorizacion_dashboard` y el endpoint REST asociado. No se permiten lecturas cruzadas a otras tablas. Si el Dashboard requiere más información, debe solicitarse un nuevo endpoint formalmente, no acceso directo a las tablas transaccionales.

### RN-09 — TTL y reanudación de sesión (nueva)

Toda sesión tiene un TTL de 5 minutos medido sobre `ultima_actividad`. Requests con sesión fuera del TTL son rechazados con HTTP 401. Al recargar la página, el cliente invoca `POST /api/auth/resume` **antes** de emitir cualquier otro request autenticado; el frontend se bloquea (loader "Cargando…") hasta recibir respuesta. Esta secuencia evita la carrera entre el beacon de `pagehide` (que marca `activa=0`) y el resume (que reactiva): si cualquier catálogo o recurso se solicitara antes del resume, el 401 resultante forzaría un logout indeseado.

### RN-10 — Inmutabilidad de los snapshots (nueva)

Los campos `ingenieros_snapshot`, `jdts_snapshot`, `jefes_snapshot` son inmutables tras el `INSERT` del registro. Ni las ediciones en estado `'borrador'` ni el archivado al histórico los modifican. El `INSERT` en `registro_historico` debe copiar los snapshots del `registro_activo` tal cual, sin recomputarlos. Esto es esencial para garantizar trazabilidad histórica exacta.

### RN-11 — DISP no permite cambios consecutivos al mismo estado (F12)

Aplica a RF-055 (POST) y RF-056 (PUT cuando `evento` cambia). Si el evento nuevo coincide con el evento del registro vigente (POST) o con el del N-1 último histórico (PUT), el backend rechaza con `409 mismo_estado` (POST) o `409 mismo_estado_que_anterior` (PUT). Motivo: una "secuencia" `En Servicio → En Servicio` no representa un cambio operativo y rompe la utilidad del historial. Importante (D-024): la comparación es por string `evento`, no por `codigo`. Por eso `Indisponible → Mantenimiento` (ambos `codigo=-1`) **es válido** — cambia el evento aunque el código no.

### RN-12 — Cronología estricta para `fecha_inicio_estado` en DISP (F12)

`fecha_inicio_estado` debe cumplir dos condiciones combinadas:

1. **Estrictamente posterior** al vigente (POST) o al N-1 último histórico (PUT) → `409 fecha_anterior_a_vigente` (POST) o `409` (PUT) si se viola.
2. **Anterior o igual** a `GETDATE()` (Bogotá) → `422` si la fecha es futura (los eventos se registran *después* de ocurrir en planta).

Cuando un PUT cambia la `fecha_inicio_estado` del vigente, el backend ejecuta un side-effect controlado en `registro_historico`: `UPDATE registro_historico SET fecha_fin_estado = nueva_fecha WHERE registro_id = N-1.registro_id`. Esta es la **única excepción documentada** a la regla de "histórico inmutable" (la sección 7.8 del MODBD lo ratifica).

### RN-13 — CIET de cierre MAND con autor SISTEMA y snapshots agregados del día (F16)

Aplica a RF-061 (cierre automático MAND). El CIET emitido por el sweeper diario tiene `creado_por = USUARIO_SISTEMA_ID` (usuario seedeado al arranque, ver MODBD §2.3.2) y los snapshots `ingenieros_snapshot`/`jdts_snapshot`/`jefes_snapshot` se construyen como agregados del día completo — toda la guardia que rotó por la grilla MAND, no solo la sesión activa al momento del cron. Esto difiere del patrón de CIET regular emitido por F4 cierre cronológico (`registrarEventoCierre`) donde el autor es el usuario que cierra y los snapshots son del momento. `campos_extra` incluye `{ rol: 'SISTEMA', forzado: true, motivo: 'mand-sweeper-diario', fecha_cerrada, registros_cerrados, bitacora_origen }` para auditoría.

**Motivo:** el cierre MAND no tiene "usuario que cierra" — es un proceso automático sin sesión humana. Forzar un usuario humano (ej. el último que editó) sería arbitrario y rompería la trazabilidad. SISTEMA marca explícitamente la naturaleza automática; los snapshots agregados preservan la lista completa de operadores que registraron eventos durante el día (información útil para auditoría operativa).

### RN-14 — REDESP solo edita periodo actual y posteriores (F16)

Aplica a RF-060 y RF-063. El operador no puede cargar redespacho para periodos pasados del día. Calculado server-side como `periodo_actual = floor(horaBogota) + 1` (P1 cubre 00:00..00:59, P15 cubre 14:00..14:59, etc.). Si `tipo='REDESP' && valor_mw != null && periodo < periodo_actual` el server rechaza con `400 periodo_bloqueado` por celda en el array `errores`. AUTH y PRUEBA NO tienen este lock — pueden registrarse a-posteriori dentro del mismo día.

**Motivo:** el redespacho es una orden operativa al despacho real; cargarlo para un periodo ya pasado no tiene sentido operativo (no se puede cambiar el despacho que ya ocurrió). AUTH y PRUEBA, en cambio, son notificaciones administrativas que el operador puede registrar después de haber sucedido el evento. El frontend muestra el lock visual pero el backend lo enforce con `periodo_actual` recalculado en cada POST — una manipulación cliente no puede saltearlo.

**Alcance del lock en la corrección (D-057):** al corregir un lote (RF-071) el lock se evalúa **sobre el delta**, no sobre el estado: rebota si un periodo pasado **cambia de valor**, **se agrega** o **se quita** (quitarlo retira lo publicado, que es un cambio de valor), y deja pasar los periodos pasados que quedan **idénticos**. **No aplica al borrado del lote completo** (RF-072): borrar es corregir un registro errado, no reescribir un despacho pasado.

**Alcance del lock (D-056):** protege **el valor en MW y nada más**. La **hora de la llamada** (RF-068), el funcionario y la descripción del lote se registran siempre, aunque la llamada haya ocurrido hace 45 minutos: el lock existe para no falsear el despacho, no para censurar lo que se documenta. Un lote de REDESP con **todos** sus periodos bloqueados se rechaza entero (`periodo_bloqueado`) y **no se escribe nada** — pero eso ya no puede hacer perder el comentario en silencio, porque la metadata viaja con el lote y no se aplica por separado.

### RN-16 — Operación 24h es append-only: registrar agrega, nunca pisa (D-056)

Aplica a RF-060, RF-068, RF-069 y RF-070. Cada Guardar de la grilla MAND **inserta** registros nuevos e inmutables; ninguna operación de la grilla modifica ni borra lo ya registrado. En consecuencia **pueden coexistir varios registros** para el mismo `(tipo, periodo, día, planta)` —el CND llama varias veces por el mismo periodo, con valores, funcionarios y motivos distintos— y todos quedan en la bitácora. Lo que el dashboard publica se **recalcula por celda** entre los registros vivos (RF-069), así que "varios registros" no significa "varios valores publicados". El guardado es **atómico**: un solo error rechaza todos los lotes del Guardar, no se escribe nada y **la grilla conserva lo capturado**.

**Motivo:** el modelo anterior representaba el periodo como una celda de hoja de cálculo con un valor único, y la segunda llamada del día pisaba a la primera — la operación real se perdía. Que registrar sea aditivo es lo que hace del apartado una **bitácora** y no una planilla. El costo consciente fue que corregir un error de digitación exige un camino aparte — **ese camino ya existe: RN-17 / RF-071 / RF-072 (D-057)**, fuera de la grilla y por lote.

### RN-17 — Corregir en Operación 24h es por lote, quirúrgico y compartido (D-057)

Aplica a RF-071 y RF-072. Corregir **no es capturar**: vive fuera de la grilla (que sigue naciendo vacía), actúa sobre **el lote completo** y nunca sobre un periodo suelto, y está abierta a **cualquiera de los dos cargos que escriben la bitácora, sea o no el autor** — excepción acotada a MAND de la regla "solo el autor" (D-049), que se implementa como gate por `puede_crear` y no como bypass por cargo. Tres invariantes que la sostienen:

1. **El diff conserva las filas que sobreviven** (`registro_id`, `creado_por`, `creado_en`). Un reemplazo del lote entero dejaría huérfano `evento_dashboard.registro_origen_id`, que **no admite FK** porque el origen migra entre `registro_activo` y `registro_historico`.
2. **Un lote no se parte entre dos días Bogotá**: las celdas que se agregan **heredan** el `fecha_evento` del lote (que identifica el día del lote, no el instante de escritura — esa atribución vive en `modificado_por`/`modificado_en`), y su `turno_id` se resuelve por el **periodo**, jamás por el momento de la corrección.
3. **Nunca un 200 mentiroso**: si tras el diff el lote se queda sin ninguna celda, se rechaza con `lote_sin_celdas` en vez de escribir a medias o de borrar por implicación. Borrar es una acción explícita y confirmada (RF-072).

**Motivo:** un error de digitación en MAND no es cosmético — se publica al dashboard de generación. Y el turno rota: exigir que corrija el autor significaría que un error registrado a las 17:55 queda sin arreglo cuando su autor sale a las 18:00.

### RN-18 — El asiento es del sistema: fuente única, copia derivada (D-058)

Aplica a RF-070, RF-073, RF-074 y RF-075. El texto de un evento se redacta **una sola vez y del lado del servidor**, y las tres salidas —listado del día, bitácoras de Sala y libro mensual— lo consumen sin reescribirlo. Cuatro invariantes:

1. **El origen manda; la copia jamás diverge.** La única fuente de verdad es el evento de Operación 24h: corregirlo regenera el asiento en sus dos copias y borrarlo las borra (RQ-02.6/7/8). Por eso **la copia no se edita ni se borra en su destino, tampoco por su autor** — que es el del origen, y a quien la regla "solo el autor" (D-049) le habría dado permiso. Es una **restricción**, no un bypass: recorta quién edita, no lo amplía, y no exceptúa a nadie.
2. **La cascada alcanza solo lo vivo.** Si las copias ya se archivaron por cierre de turno, el histórico **no se reescribe** (RF-032) y la corrección del origen procede igual. Rechazarla volvería incorregible un lote **por el estado de su reflejo**, invirtiendo la jerarquía.
3. **Un tipo sembrado es un tipo tecleable, salvo que se impida.** Los tipos espejo llevan `seleccionable = 0` y el filtro se aplica **en el selector y en el endpoint de escritura**: esconder una opción no impide postear su id, y una fila "de reflejo" **sin origen** sería indistinguible de una real e imposible de rastrear — la doble digitación que este requerimiento elimina.
4. **El texto se genera, no se inventa.** El sistema solo redacta eventos que alguien registró: si la rutina del turno no se escribe en las bitácoras de Sala, esas filas **no existen** en el libro. Es adopción, no software.

### RN-15 — Convención TZ: BD en UTC, presentación Bogotá (F19+F20+F21+F22)

Aplica a TODAS las RFs que involucran timestamps. La BD guarda DATETIME2 en UTC con `SYSUTCDATETIME()`. Toda conversión a Bogotá ocurre en presentación (frontend con `Intl.DateTimeFormat('America/Bogota')` con `timeZone` siempre explícito) o en backend con `DATEADD(HOUR, -5, …)` o helpers `colombiaParts`/`fechaBogotaStr`/`fechaBogotaIso`. Aplicación opera para usuarios en Colombia: render Bogotá explícito en TODO formatter, no la TZ del navegador.

**Motivo:** UTC es el instante absoluto sin ambigüedad; sortear y comparar fechas funciona correctamente sin importar DST. Colombia no tiene DST hoy, pero la convención protege ante futuras políticas. Además, el driver `mssql` con `useUTC=true` (default) ya asume UTC — alinearse evita fricción. Los formatters frontend con `timeZone` explícito eliminan dependencia de la zona del navegador (operador remoto u host con TZ no-Bogotá ve siempre hora planta).

**Restricciones:**

- Inputs `<input type="datetime-local">` (modal DISP "Cambiar estado") interpretan como hora Bogotá. Frontend convierte a UTC con offset `-05:00` apendido antes de POST (patrón `toIsoFromLocal`/`toDatetimeLocal` post F20).
- Helpers prohibidos en código nuevo: `getTimezoneOffset()`, `new Date(d.toLocaleString(...))`, `.toLocaleString` sin `timeZone` explícito, `getHours()`/`getDate()` sin offset previo.
- `GETDATE()` está prohibido en `Bit-cora-g3/server/` salvo en migraciones legacy comentadas. Toda escritura DATETIME2 nueva usa `SYSUTCDATETIME()`. Sweep TTL post F19 también lo usa.
- CIET `campos_extra.fecha_cerrada` (RF-061) es fecha Bogotá (`YYYY-MM-DD`), `fecha_revertida` (RF-057) es ISO con sufijo `-05:00` Bogotá. CIETs pre-F19 quedan en UTC; distinguir por fecha de creación si auditás históricos cruzados.
- SSMS: usar columnas calculadas `*_bogota` (MODBD §4.5) o snippets en `Bit-cora-g3/sql/snippets/` para inspección humana.

Cross-ref: MODBD §7.10 (convención completa con antipatrones) y §4.5 (columnas calculadas).

---

## 8. Supuestos, restricciones y exclusiones

### 8.1 Supuestos

- El reloj del servidor de base de datos está sincronizado con fuente NTP confiable. La hora Colombia se deriva mediante aritmética UTC−5 sin horario de verano.
- Los usuarios operan desde navegadores modernos (Chrome, Edge, Firefox últimas 2 versiones) con soporte para `navigator.sendBeacon()` y eventos `pagehide`. No se soporta Internet Explorer ni versiones antiguas de Safari.
- La red interna de Gecelca permite comunicación entre los puestos de la sala de control, el servidor de aplicación y el servidor SQL sin restricciones de firewall inesperadas.
- El Dashboard de Generación está preparado para consumir el endpoint REST de autorizaciones y modificar su cálculo de desviación en consecuencia.

### 8.2 Restricciones

- **Motor de base de datos:** SQL Server 2019 o superior (uso de `OPENJSON`, `JSON_VALUE`, `FIRST_VALUE` con window functions, `DATEADD` para cálculo de TTL).
- **Stack del backend:** Node.js con ESM, módulo `http` nativo, `mssql` como driver. Mismo stack que el Dashboard de Generación existente.
- **Autenticación** simple por email+contraseña hasta que se complete la integración con Keycloak.
- **Persistencia de sesión:** `sessionStorage` (no `localStorage`) — por diseño, cerrar la pestaña fuerza re-login.

### 8.3 Exclusiones del alcance

- Notificaciones por correo electrónico o en tiempo real (WebSocket push) a usuarios sobre bitácoras sin cerrar. Queda como requerimiento futuro.
- Exportación de reportes a PDF/Excel.
- Gestión de archivos adjuntos (fotos, diagramas) en registros.
- Aplicación móvil nativa.
- Sincronización offline o trabajo sin conexión.
- Multi-tenancy o soporte para plantas adicionales fuera de Gecelca 3 / 3.2 (la estructura de datos lo soporta, pero no es requerimiento inmediato).
- Refresh tokens o persistencia de sesión más allá del cierre de pestaña.

---

## 9. Criterios de aceptación global

La versión 1.1 del módulo se considerará aceptada cuando se verifiquen, mediante pruebas manuales y automatizadas, los siguientes criterios:

1. Todos los requerimientos funcionales (**RF-001 a RF-053**, incluyendo los nuevos RF-005, RF-006, RF-007) están implementados y pasan sus casos de prueba.
2. Los datos semilla definidos en BIT-MODBD-2026-001 se cargan automáticamente al inicializar la base de datos (10 bitácoras, 4 cargos, 2 plantas, 2 usuarios, matriz completa de permisos).
3. Un Jefe de Turno puede completar un ciclo diario: iniciar sesión, crear al menos un registro en cada bitácora general, crear una autorización en AUTH, verificar que aparece vía `GET /api/autorizaciones`, cerrar todas las bitácoras (individual o masivamente) y consultar los históricos del día.
4. Un Ingeniero de Operación puede crear, editar y eliminar registros en las bitácoras permitidas, y NO puede crear en Disponibilidad, Autorizaciones o Planta de Agua.
5. Un Ingeniero de Planta de Agua ve únicamente la bitácora de Planta de Agua en la interfaz.
6. Un Gerente de Producción ve todas las bitácoras pero no tiene controles de escritura visibles.
7. Una autorización creada desde la interfaz aparece en el endpoint público `/api/autorizaciones` en menos de 2 segundos.
8. El cierre de una bitácora archiva los registros al histórico con `fecha_cierre_operativo` correcto, **preservando los snapshots originales sin recomputarlos**, y los registros originales desaparecen de `registro_activo`.
9. La búsqueda en históricos por texto libre en `detalle` retorna resultados coherentes y paginados correctamente. La búsqueda textual sobre nombres de ingenieros opera contra `ingenieros_snapshot` sin JOINs a usuarios.
10. Agregar una nueva bitácora mediante `INSERT` directo en `lov_bit.bitacora` con su JSON `definicion_campos` resulta en que la nueva bitácora aparezca automáticamente en el frontend tras recargar, con su formulario renderizado correctamente, sin modificación de código.
11. **[Nuevo v1.1] Reload preserva sesión:** un usuario autenticado que presiona F5 vuelve a la UI con su contexto intacto (misma planta, mismo cargo), sin pasar por la pantalla de login, siempre que el reload ocurra dentro del TTL de 5 minutos. La transición presenta un loader breve ("Cargando…").
12. **[Nuevo v1.1] Cierre de pestaña fuerza re-login:** abrir una pestaña nueva después de cerrar la anterior requiere re-autenticación. Se verifica que `bitacora.sesion_activa.activa = 0` tras el `pagehide`.
13. **[Nuevo v1.1] TTL expirado rechaza requests:** una sesión cuya `ultima_actividad` esté fuera del TTL produce HTTP 401 en el siguiente request, y el cliente hace logout automático.
14. **[Nuevo v1.1] Snapshots inmutables:** desactivar (`activo=0`) un ingeniero en `lov_bit.usuario` no modifica el contenido de `ingenieros_snapshot` en registros ya creados. El histórico refleja la realidad del momento.
15. **[Nuevo v1.1] Eliminar un usuario físicamente** (fuera del flujo normal) no rompe la integridad de los registros históricos — los snapshots son autocontenidos.

---

## 10. Historial de versiones

| Versión | Fecha | Autor | Cambios |
|---|---|---|---|
| 1.0 | Abril 2026 | Ingeniería de Software | Versión inicial del documento de requerimientos funcionales. |
| **1.1** | **Abril 2026** | **Ingeniería de Software** | **Incorpora los cambios de arquitectura de sesión y snapshots:**<br>• Agrega **RF-005** (logout vía `sendBeacon` en `pagehide`), **RF-006** (reanudación por `POST /api/auth/resume`) y **RF-007** (enforcement de TTL server-side + barrido en `initDB`).<br>• Reemplaza las referencias a FK de roles (`ingeniero_id`, `jdt_turno_id`, `jefe_id`) por **snapshots JSON inmutables** en RF-020, RF-021, RF-022, RF-024, RF-030, RF-041, RF-050, RF-051.<br>• Reescribe **RN-02** y **RN-03** para reflejar el modelo de snapshot.<br>• Agrega **RN-09** (TTL y reanudación) y **RN-10** (inmutabilidad de snapshots).<br>• Actualiza sección **3.3** a "Resolución de snapshots de roles" con predicado TTL explícito.<br>• Agrega sección **3.4** (Ciclo de vida de la sesión) con diagrama implícito de estados y eventos.<br>• Actualiza **matriz de trazabilidad** (sección 6) con los nuevos campos.<br>• Agrega criterios de aceptación **11 a 15** para reload, logout de pestaña, TTL expirado, snapshots inmutables y robustez ante eliminación de usuarios. |
| **1.2** | **2026-04-29** | **Ingeniería de Software** | **F12+F13+F14 — Disponibilidad como mini-dashboard:**<br>• Agrega sección **4.7** con **RF-054** (vista mini-dashboard), **RF-055** (POST DISP transaccional con cierre automático), **RF-056** (PUT DISP con side-effect en histórico), **RF-057** (deshacer último con CIET audit), **RF-058** (visibilidad universal + creación restringida), **RF-059** (endpoint cross-app para Dashboard productivo).<br>• Agrega **RN-11** (no consecutivos iguales) y **RN-12** (cronología estricta) — usan numeración 11/12 porque RN-09/RN-10 ya estaban ocupadas en v1.1.<br>• Documenta que el side-effect del PUT sobre `N-1.fecha_fin_estado` es la **única excepción** a la regla de histórico inmutable (cross-ref con MODBD §7.8). |
| **1.3** | **2026-05-04** | **Ingeniería de Software** | **F16+F17+F18 — Sala de Mando con batch save y cierre automático:**<br>• Agrega sección **4.8** con **RF-060** (batch save atómico vía `POST /api/sala-de-mando/guardar`), **RF-061** (cierre automático fin de día vía sweeper diario con autor SISTEMA), **RF-062** (multi-select Excel-like para replicar valor), **RF-063** (lock REDESP por hora actual con auto-update 60s), **RF-064** (FuncionarioCND condicional por tipo), **RF-065** (exclusión de cierre individual y masivo).<br>• Agrega **RN-13** (CIET de cierre MAND con autor SISTEMA y snapshots agregados del día) y **RN-14** (REDESP solo edita periodo actual y posteriores).<br>• Cross-ref con MODBD §2.3.2 (usuario SISTEMA), §4.4 (`migracion_aplicada` + `mand_cierre_log`) y §7.9 (políticas MAND).<br>• La paginación entre días MAND (F10) fue eliminada — la grilla solo opera sobre HOY. El endpoint `GET /api/sala-de-mando/dias-pendientes` se borró en F16. |
| **1.4** | **2026-05-05** | **Ingeniería de Software** | **F19+F20+F21+F22 — corrección de fechas (convención TZ):**<br>• Agrega **RN-15** (BD en UTC, presentación Bogotá explícito) — aplica a todas las RFs con timestamps.<br>• Cross-ref con MODBD §7.10 (convención completa con antipatrones) y §4.5 (columnas calculadas `*_bogota` para SSMS).<br>• No agrega RFs nuevos — la convención formaliza el comportamiento de RFs existentes (RF-005..RF-007 sesiones/TTL, RF-055..RF-058 DISP, RF-060..RF-063 MAND, RF-061 CIET sweeper).<br>• Bugs corregidos sin cambios contractuales: T1 (`GET /api/sala-de-mando` con `DATEADD(HOUR, -5, fecha_evento)`), T2 (sweep TTL `db.js` con `SYSUTCDATETIME()`), T3 (CIET `fecha_cerrada`/`fecha_revertida` en hora Bogotá explícita).<br>• Deuda documentada: T4 cierre cronológico edge case, tests de componentes RTL, CI matrix con TZ alternos. |
| **1.5** | **2026-05-19** | **Ingeniería de Software** | **Conformación de turno (D-025):**<br>• Agrega **RF-066** (snapshot histórico al cierre de turno + endpoints `GET /api/conformacion-turno` y `POST /api/conformacion-turno/trigger`).<br>• Fix retro: `POST /api/auth/logout` ahora pobla `sesion_activa.cerrada_en` (era deuda F2 nunca cerrada). Sin nuevo RF — completa el comportamiento esperado de RF-004.<br>• Cross-ref con MODBD §4.7 (DDL formal de `conformacion_turno` y trigger híbrido). |
| **1.6** | **2026-07-03** | **Ingeniería de Software** | **Finalizar turno revertible (D-040):**<br>• Agrega **RF-067** (finalizar/revertir turno + inhibición de registro en bitácoras generales).<br>• La finalización pasa a `sesion_activa.turno_finalizado_en` (fuente única, revertible vía `POST /api/bitacora/revertir-turno`); `sesion_bitacora.finalizada_en` queda como SOLO presencia — corrige el bug donde *ver* una bitácora des-finalizaba el turno (`/abrir` la reseteaba).<br>• Write-gate `409 turno_finalizado` solo en la rama genérica de `/api/registros` (MAND/DISP/COMB exentos). Front deriva del backend (sin `localStorage`).<br>• Cross-ref con MODBD §3 (`turno_finalizado_en`) y §4.6 (semántica de `finalizada_en`). |
| **1.7** | **2026-07-03** | **Ingeniería de Software** | **Eliminación del cierre individual (D-042):**<br>• **RF-030** (cierre individual de bitácora) queda **RETIRADO**: se elimina `POST /api/cierre/bitacora`, el botón "Cerrar Turno" por pestaña, el hook y sus tests. Se conserva el número por trazabilidad.<br>• **RF-031** se reescribe como **cierre de turno** autónomo (ya no "iteración de RF-030"): `POST /api/cierre/masivo` es el ÚNICO cierre.<br>• **RF-065** se actualiza: MAND/DISP quedan excluidos por `codigo NOT IN ('DISP','MAND')`; desaparecen los rechazos `mand_cierre_individual_no_permitido` y `bitacora_no_cerrable` (ya no hay endpoint individual).<br>• Cross-ref con D-042 y D-005 (cierre cronológico, intacto). |
| **1.8** | **2026-07-22** | **Ingeniería de Software** | **Operación 24h append-only (D-056 / REQ-03):**<br>• **RF-060 reescrito**: `POST /api/sala-de-mando/guardar` deja de ser un diff y pasa a ser **captura append-only por lotes** — siempre INSERT, nunca UPDATE/DELETE; body con `hora` por fila; respuesta `200 { resumen: { lotes, registros } }`; motivos nuevos `hora_requerida`/`hora_invalida`/`hora_futura`/`lote_sin_celdas` y retiro de `detalle_sin_celdas` y del `modificado_por` selectivo en la grilla.<br>• Agrega **RF-068** (hora de la llamada al CND + lote de captura: `campos_extra.lote_id` y `campos_extra.hora_llamada`, validación de hora a nivel de fila contra el reloj del servidor con 5 min de tolerancia, clave **ausente** en lo migrado), **RF-069** (publicación al dashboard resuelta **por celda** y desde cero; sin registros → **DELETE** de la fila; soft-delete intacto en el cierre diario; UNIQUE y contrato cross-repo sin cambios) y **RF-070** (listado del día bajo la grilla, solo lectura, gated por `puede_ver`; baja del pivote `GET /api/sala-de-mando`).<br>• Agrega **RN-16** (registrar agrega, nunca pisa: varios registros por `(tipo, periodo, día, planta)`; atomicidad; la grilla conserva lo capturado ante un 400).<br>• **RF-063** acotado: el lock de REDESP protege el **valor**, nunca la hora ni la descripción (ampliado en RN-14). **RF-064** sin cambios de fondo — la regla de `funcionariocnd` se evalúa ahora por lote.<br>• §4.8 reencabezada. Migración **F32.A1** como excepción a RF-032 con respaldos residentes `registro_{historico,activo}_backup_D056`.<br>• Cross-ref con D-056, BIT-MODBD §5.1/§7.9 y REQ-03. Corrección y borrado por lote → D-057 (REQ-04). |
| **1.9** | **2026-07-26** | **Ingeniería de Software** | **Corrección y borrado por lote en Operación 24h (D-057 / REQ-04):**<br>• Agrega **RF-071** (`PUT /api/sala-de-mando/lotes/:lote_id` — corrección del lote completo con **diff quirúrgico**: conserva `registro_id`/`creado_por`/`creado_en` de las celdas que sobreviven, cambia MW, hora, funcionario, descripción y el conjunto de periodos; tipo inmutable; recálculo de la publicación por celda tocada y de **todas** si cambió la hora; motivos nuevos `periodo_duplicado` y `lote_sin_celdas`; desenlaces `404 lote_inexistente` / `409 lote_cerrado` / `403 lote_de_otra_planta`) y **RF-072** (`DELETE` del lote — borrado **real** de sus N filas con retroceso de lo publicado al lote anterior vigente, o eliminación de la fila de `evento_dashboard` si no queda ninguno).<br>• Agrega **RN-17** (corregir es por lote, quirúrgico y compartido: el diff conserva las filas por la ausencia de FK en `registro_origen_id`; `fecha_evento` heredada para no partir el lote entre dos días Bogotá; nunca un 200 mentiroso).<br>• **Excepción acotada a D-049** documentada donde de verdad vive: en el gate `puede_crear` del endpoint, **no** en `canEditarRegistro` (MAND no pasa por ese helper) — `permissions.js` **no se tocó** y el resto de bitácoras conserva "solo el autor".<br>• **RN-04.d resuelta (REQ-04 §8.2):** en la corrección **cualquier** cambio sella `modificado_por`/`modificado_en`; el criterio selectivo de **D-019** queda vigente solo en la captura.<br>• **RN-14 ampliada:** el lock de REDESP se evalúa **sobre el delta** al corregir y **no aplica** al borrado del lote completo.<br>• **RF-070 actualizado** (el listado suma acciones; el endpoint sigue siendo de solo lectura) y §4.8 reencabezada. **Sin DDL ni migración** — BIT-MODBD **no cambia**.<br>• Fuera de alcance, sin cambios: el **formato de mensaje** de WhatsApp (REQ-04 §8.1, falta la plantilla → D-058) y la **cascada a SALAJDT/SALAING** (REQ-02, esas copias no existen todavía).<br>• Cross-ref con D-057, D-056, D-055, D-049, D-019 y REQ-04. |
| **2.0** | **2026-07-27** | **Ingeniería de Software** | **Asientos normalizados de operación (D-058 / REQ-02 + REQ-04 + REQ-06):**<br>• Agrega **RF-073** (motor de asientos **server-side y puro**: plantillas por tipo, convenciones canónicas de unidad/potencia/periodos, paso literal de lo escrito en Sala con prefijo anti-duplicado; la hora **nunca** va en el texto; lanza donde el texto se persiste y degrada donde solo se muestra), **RF-074** (**reflejo** del lote de Operación 24h a `SALAJDT` **y** `SALAING` dentro de la transacción del origen, con cascada al corregir y al borrar) y **RF-075** (**libro mensual GENE-F03** descargable: `GET /api/sala-de-mando/reporte-mensual?mes=YYYY-MM`, una hoja por día, tres bloques de turno, las dos unidades mezcladas, plantilla real clonada y **cero dependencias nuevas**).<br>• Agrega **RN-18** (el asiento es del sistema: fuente única, copia derivada — el origen manda, la cascada alcanza solo lo vivo, un tipo sembrado es tecleable salvo que se impida, y el texto se genera pero no se inventa).<br>• **RF-070 actualizado**: cada lote del listado viaja con su campo `asiento` y el renglón suma **copiar renglón** / **copiar el día** — copiar **no es escribir**, así que los botones no exigen `puede_crear` (cierra REQ-04 §8.3).<br>• **DDL:** una sola columna, `lov_bit.tipo_evento.seleccionable` (`F34.A1`) + 8 tipos espejo con `seleccionable = 0` — ver **BIT-MODBD v2.3 §2.5 y §7.11**. **Sin migración de datos** y **sin cambios en el contrato cross-repo**.<br>• **Bloqueantes cerrados:** REQ-04 §8.1 (el formato de mensaje que arrastraba desde D-057), REQ-02 §8.1 y §8.2, REQ-06 §8.1 a §8.4.<br>• Fuera de alcance, con ADR propio pendiente: el **reflejo de Disponibilidad** (crear/editar/**deshacer con copia anulada**, RQ-02.12) — sus tipos espejo ya están sembrados.<br>• Cross-ref con D-058, D-057, D-056, D-055, D-049, D-045, REQ-02 / REQ-04 / REQ-06 y `FORMATO-ASIENTOS-OPERACION.md`. |

---

**FIN DEL DOCUMENTO DE REQUERIMIENTOS FUNCIONALES**
