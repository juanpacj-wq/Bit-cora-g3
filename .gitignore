node_modules/
dist/
.env
server/.env

# Archivos sueltos en la raíz (insumos van a docs/requerimientos/formatos/)
/*.xlsx
/*.png

# Ajustes locales de Claude Code (settings.json con el deny de git SÍ se versiona)
.claude/settings.local.json
